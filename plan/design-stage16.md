# Stage 16 設計書: calc と sim の 2 モデル化

- 対象: `D:\nikke_project`（要件は `plan/requirements.md`, `plan/roadmap.md`）
- 状態: **承認済み（2026-09-25。7 節の 7 点とも推奨案）。16-A 実装済み（2026-09-25。実装での差分は 8 節、検証は [verification.md](verification.md) Stage 16 節）**。**16-B は 9 節（2026-09-26 承認・6 点とも推奨案。同日実装。実装での差分と確かめは 9.7 節）**。16-C は未着手。
- 関連: [roadmap.md](roadmap.md)、[design-stage5.md](design-stage5.md)（sim / calc の最初の分担）、[design-stage10.md](design-stage10.md)（1 パス目のフレームループ）、[design-stage12.md](design-stage12.md) 5 節（Stage 15 の実戦との突き合わせ）、[verification.md](verification.md)
- 作成日: 2026-09-25

## Context

いまの公開サイト（`apps/calc`）は calc モデル（`team.ts` の `computeTeamDamage`）だけを使い、sim（`sim/engine.ts` の `runSimulation`）は CLI と整合テストからしか呼ばれない。これを **2 つのモデルとして育て、サイトからどちらも使えるようにする**。

2026-09-25 のユーザー決定:

1. **sim が calc より正確になるもの**
   - 状態に依存する機構: **必須**。実戦ではさまざまな要因がダメージを増減させ、期待値モデルでは再現しにくいケースがある。
   - 乱数（ばらつき）: **将来必要**。期待値だけでなく、どれだけばらつくかを出す。
   - 内訳の表示: あれば良い程度。ただし、ダメージに絡む出来事（バリア・無敵・画面外への移動など）は可視化する。
2. **アプリ**: 当面は 1 つのアプリで calc / sim を切り替える。開発に余力ができたら 2 つのアプリに分ける。
3. **名前**: モデルとアプリを 1 対 1 に揃え、「calc = calc モデルとそのアプリ」「sim = sim モデルとそのアプリ」とする。

---

## 0. 設計前に分かったこと（2026-09-25）

### 0.1 いまの sim と calc

| 項目                                             | sim                                                                                | calc                                          |
| ------------------------------------------------ | ---------------------------------------------------------------------------------- | --------------------------------------------- |
| 1 パス目（射撃の列・時刻表・射撃に効くバフ・CT） | `team.ts` の `planTeamRun`（中身は `sim/firstPass.ts` のフレームループ）。**共通** | 同左                                          |
| 2 パス目                                         | フレームを回し、区間ごとの 1 トリガー値を足す                                      | 区間ごとに「射撃数 × 1 トリガー値」で合計する |
| 1 発分の式                                       | `damage.ts` の `computeTriggerDamage`・`computeBurstHit`。**共通**                 | 同左                                          |
| 差                                               | 発射サイクルの離散化（マガジンの位相と端数）だけ（`__tests__/simCalc.test.ts`）    |                                               |

### 0.2 実行時間（Node、5 体・180 秒・スペック固定、20 回の平均）

| 編成                           | sim   | calc  |
| ------------------------------ | ----- | ----- |
| 82, 330, 271, 870, 225（主力） | 35 ms | 27 ms |
| 10, 101, 160, 172, 191         | 12 ms | 7 ms  |

**いまは速さも精度もほとんど変わらない。** calc も 1 パス目でフレームループを回しているため。2 つに分ける意味が出るのは、sim に calc で表せないもの（1 節）を入れてから。

### 0.3 名前の重なり

- 「calc」がアプリ（`apps/calc`）とモデル（`team.ts` の `computeTeamDamage`）の両方を指す。
- `team.ts` の中に、共通の部分（`planTeamRun`・`planSkillHits` など）と calc モデル（`computeTeamDamage`）が混ざっている。
- 共通のフレームループ（`firstPass.ts`）が `sim/` にあり、calc も使う。

---

## 1. 2 つのモデルの役割

|                        | calc                                                     | sim                                                                      |
| ---------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------ |
| 位置づけ               | すぐ結果が出る期待値モデル。入力を変えるたびに再計算する | 正確さを優先するフレーム逐次モデル（実行のきっかけは 7 節 6）            |
| 実行                   | 乱数なしで 1 回                                          | 乱数なしで 1 回（期待値モード）、または乱数ありで N 回（分布モード）     |
| 合計の出し方           | 区間ごとの期待値                                         | 1 発ごと                                                                 |
| 決定論的な状態         | 共通のフレームループで扱う（sim と同じ）                 | 同左                                                                     |
| 乱数の結果で変わる状態 | 扱わない（未対応・近似の一覧に出す）                     | 扱う（会心・コア命中・命中が起点のトリガーやスタックなど）               |
| 出力                   | 合計・枠別・区間別の内訳                                 | calc と同じ内訳 ＋ 分布（平均・標準偏差・分位点）＋ 出来事のタイムライン |

**芯になる原則**:

- **式・スキル定義・データ・フレームループは 1 つだけ持つ。** 2 つのモデルで違うのは「乱数を振るか」と「区間でまとめるか・1 発ずつ足すか」だけにする。機構を 2 か所に書くと、どちらかが古くなるため。
- **決定論的な状態依存（敵の出来事、経緯で変わるスキル）は共通のフレームループに入れる。** その結果、calc にも効く。calc に入らないのは、乱数の結果に依存する状態だけ。
- **sim の期待値モードと calc の差は、発射サイクルの離散化だけに保つ。** これを今の `simCalc.test.ts` と同じく回帰テストの軸にする。乱数ありの N 回の平均も、期待値モードに収束することをテストする。

---

## 2. コードの構成

```
packages/core/src/
  damage.ts, stats.ts, skills/, burst/ …   共通（今のまま）
  frame/     共通のフレームループ（今の sim/firstPass.ts・shooter.ts・firing.ts・shots.ts と team.ts の planTeamRun など）
  calc/      calc モデル（今の team.ts の computeTeamDamage と区間の集計）
  sim/       sim モデル（今の sim/engine.ts。16-C で乱数と分布）
apps/web/    1 つのアプリ（今の apps/calc）。calc / sim を切り替える
```

- `team.ts` は共通の型と入力の検証だけにする。
- 2 つのアプリに分けるときは、`apps/web` を `apps/calc` と `apps/sim` に分け、入力の画面を `packages/ui` に切り出す。
- 公開 URL（https://hkhkm-dev.github.io/nikke_project/）は変えない。

---

## 3. 段取り（1 段 1 PR）

### 16-A 構造の整理（挙動は変えない）

- 2 節の通りにファイルを移し、`team.ts` を分ける。**数値は 1 つも変わらない**（全テストがそのまま通る）ことを完了条件にする。
- `apps/calc` を `apps/web` に改名する（Pages の workflow・README・AGENTS.md も合わせる）。
- 画面に「calc / sim」の切り替えを足す。sim の結果は calc と同じ内訳の表で出し、calc との差を並べる。sim はブラウザでも数十 ms で終わるので、この段ではメインスレッドで実行する。

### 16-B 敵の出来事（バリア・無敵・画面外）

- 敵の出来事の時刻表を core の共通の入力にする（`EnemyEvent`: 種類・開始・終了、仮称）。敵のプリセット（Stage 15）に持たせる。
- 共通のフレームループで出来事を扱うので、calc と sim の両方に効く。
- **出来事の間に何が起きるかは、実測で決める**（ダメージが 0 になるか、ゲージが溜まるか、射撃の回数トリガーを数えるか、画面外の間にリロードするか、など）。単騎・最小構成で撮り、確定するまでは「未実装」のプレースホルダーにする。
- sim の結果に、出来事とダメージの推移のタイムラインを出す（ユーザー決定の「可視化」）。

### 16-C 乱数と分布（将来）

- シードを指定できる乱数で、会心・コア命中・命中を 1 発ごとに判定する（同じシードなら同じ結果）。
- N 回実行して、平均・標準偏差・分位点を出す。N 回ぶんの時間がかかる（N = 100 で数秒の見込み）ので、ブラウザでは Web Worker で回す。
- 乱数の結果で変わる状態（会心時・コア命中時のトリガーなど）は、この段以降に sim だけに入れる。
- 検証: 射撃場のスペック固定の反復のばらつき（σ ≈ 2%、[design-stage12.md](design-stage12.md) 5.3 節）と比べる。

---

## 4. 検証

| 段   | 確かめること                                                       |
| ---- | ------------------------------------------------------------------ |
| 16-A | 全テストが変更なしで通る。公開サイトの計算結果が移行前と一致する   |
| 16-B | 出来事の扱いを単騎・最小構成の撮影で確定し、verification.md に記録 |
| 16-C | 乱数ありの平均が期待値モードに収束する。分布を射撃場の反復と比べる |

---

## 5. やらないこと

- 2 つのアプリへの分割（開発に余力ができたとき。2 節の形を前提にしておく）。
- ボスの行動の全体（攻撃・パーツ破壊による行動の分岐）。16-B で扱うのは、ダメージが通るかどうかを変える出来事だけ。

---

## 6. 用語（承認後に AGENTS.md に書く）

- **calc**: 区間期待値のモデルと、その結果を出すアプリ（今は `apps/web` の calc 表示）。
- **sim**: フレーム逐次のモデルと、その結果を出すアプリ（今は `apps/web` の sim 表示）。
- **共通のフレームループ**: 両モデルが使う 1 パス目（`core/src/frame/`）。

---

## 7. 決めてほしいこと

1. **アプリ 1 つの間のディレクトリ名** — 推奨: **`apps/web`**。`apps/calc` のままだと、sim の画面も入るので名前がまた重なる。
2. **共通のフレームループの置き場** — 推奨: **`core/src/frame/`**。`sim/` の下に置いたままだと「calc が sim を使う」形になり、1 対 1 の名前と合わない。
3. **sim のエンジンの持ち方** — 推奨: **共通のフレームループにオプション（乱数・1 発ごとの記録）を足す**。sim 専用のエンジンを別に書く案は、機構を 2 か所に書くことになるので採らない。
4. **敵の出来事を calc にも効かせるか** — 推奨: **効かせる**（共通のフレームループで扱うため自然に効く）。calc は「すぐ出るが粗い」ではなく「すぐ出る期待値」で、決定論的なものは正確に扱う。
5. **段の順番** — 推奨: **16-A → 16-B → 16-C**（状態依存が必須、乱数は将来、というユーザー決定の順）。
6. **sim の実行のきっかけ** — 推奨: **16-A・16-B では calc と同じく入力のたびに自動で再計算、16-C の分布モードだけボタンで実行**。
7. **Stage 番号** — 推奨: **Stage 16**。Stage 15 の撮影（15-B 迎撃戦など）とは独立しているので、並行して進めてよい。

---

## 8. 16-A の実装（2026-09-25）

2 節の構成どおりに分けた。設計からの差分と、決めたことは次のとおり。

- **結果の型は `team.ts` に置いた。** `TeamResult`・`TeamSlotResult` などは calc と sim の両方が作る（下の `simTeamResult`）ので、calc の中ではなく共通の型にした。結果に添える `treasureOf`・`skillSupportOf` も同じ理由で `team.ts` に移した。
- **1 パス目は `frame/plan.ts`**（`planTeamRun`・`planSkillHits`・`burstSnapshotState` など）。フレームループの部品（`firstPass.ts`・`shooter.ts`・`firing.ts`・`shots.ts`）は `sim/` から `frame/` に移した。`sim/` に残るのは `engine.ts`（2 パス目）と `teamResult.ts` だけ。
- **画面は sim の結果も `TeamResult` の形で受ける。** `sim/teamResult.ts` の `simTeamResult` が、sim の区間を calc と同じグループに集計する（トリガー数は sim が撃った数で `triggerSource: 'shots'`）。そのために sim の結果にバーストスキルの発動ごとの内訳（`burst.hits`）と倍率ダメージの発動列（`skillHits`）を足した。値は 1 パス目のものなので calc と同じ。
- **計算モデルの選択欄は「編成の内訳」の先頭に置いた。** sim を選ぶと、合計と枠ごとに「calc との差」を並べる。選択は閲覧者ごとの好みなので、編成の保存形式には入れず別のキー（`nikke-web.model`）に置く。編成の保存キー `nikke-calc.team.v1` は、保存済みの編成を読めるように変えない。
- sim は 7 節 6 のとおり入力のたびに自動で回す（ブラウザでも数十 ms）。
- `.claude/launch.json` の開発サーバの名前も `web` にした。

---

## 9. 16-B の詳細（2026-09-26 起案・承認）

3 節の 16-B を、実装に入れる形に細かくする。**7 節の 7 点（承認済み）は変えない**。ここで決めるのは、最初に入れる出来事・型・挙動の決め方・画面の出し方。

### 9.0 設計前に分かったこと（2026-09-26）

**材料があるのは「射撃場 3 分モードの的の停止」だけ。** ボスの出来事（ジャンプ・画面外・無敵・バリア）は、撮った録画が迎撃戦の 3 本（録画 51〜53、16 秒撃破）しかなく、単騎・最小構成の材料が無い。

既存の 3 分モードの録画 10 本で、HUD の総ダメージ（crop 300×38 at 810,34）が 45f 以上変わらない区間を fps=60 で拾った（使い捨てのスクリプト。リロードや撃ち合いの切れ目も拾うので、下は複数人の録画で停止とみられるものだけ）。

| 録画                   | 停止の始まり（f、動画の先頭から）     | 間隔（f）              |
| ---------------------- | ------------------------------------- | ---------------------- |
| 41（残弾の OCR、既知） | 2820・5003・7116・9317・11331         | 2183・2113・2201・2014 |
| 48（15-A の編成）      | 2448・4631・（7000 台は拾えず）・9198 | 2183・—・—             |
| 49（同じ編成）         | 2855・5042・7424・9605                | 2187・2382・2181       |
| 50（同じ編成）         | 2859・5046・7428・9614                | 2187・2382・2186       |

- 長さは 94〜123f（約 2 秒、録画 41 の残弾で確認）。間隔はおよそ 2,000〜2,400f（約 36 秒）で、一定ではない。
- **同じ編成の録画 49・50 は 4f 以内で一致**する一方、同じ編成の録画 48 は同じ間隔のまま約 410f 早い。どの録画も戦闘前の演出（f500〜900 の HUD の変化）は同じ位置にあるので、動画の先頭のずれでは説明できない。**時刻だけで決まる時計ではなく、的の行動（反撃の周期など）に結びついている**とみられる。位相を決めるものは分かっていない。
- 停止中に分かっていること（[design-mg-fire-rate.md](design-mg-fire-rate.md) 4 節、録画 41）: HUD の総ダメージが止まり**編成全員が撃たない**。MG は撃ち直しで**スピンアップを最初からやり直す**（CDN の `rateOfFireResetTime`、いまの sim は使っていない）。5 回のうち 2 回は**停止中に AI がリロード**した（残弾 227・81 のとき。100・268・48 のときはしない。規則は不明）。
- 影響の大きさ: 紅蓮BS の録画 46・47 で sim と実機の差 +8.7% / +6.1% のうち約 5.6% がこの停止（verification.md Stage 11 紅蓮BS 節）。15-A（録画 48〜50）の sim +21% にも同じ分が入っている。

### 9.0.1 承認時にユーザーから（2026-09-26）

- 撃てない期間は、**敵が画面外へジャンプする**ため。ジャンプには移動を伴い、**着地後は敵との距離が変わる**。
- 敵がジャンプのモーションに入ってから移動が終わるまで、**AUTO の射撃は中断**される（撮影は AUTO なので全員が止まる）。**ジャンプ中はオートバーストも発動しない**ので、バーストの発動がずれる原因にもなる。
- 攻撃できる敵がいないと**ハイド**（遮蔽物に隠れる）する。ハイド中は可能ならリロードするので、**リロード時間が一定以内なら、ジャンプのたびにリロードが終わる**。リロードが長いキャラは、終わる前に敵のジャンプが終わって撃ち始めるので、**リロードは終わらない**。

録画 41 のクラウンの残弾で裏が取れた: 停止中に込め直した 2 回（残弾 227・81 → 300）はリロード速度▲の中で、込め直しに約 95f（素の 2.5 秒 = 150f が約 37% 縮んだ値）。込め直さなかった 3 回は素のリロード（150f）で、2 秒の窓に収まらない。

### 9.1 最初に入れる出来事

**射撃場 3 分モードの的の停止**を、敵の出来事の最初の 1 つにする。

- 材料があり、単騎で撮れる（撮影場所が射撃場なので、いつでも撮り直せる）。
- 検証済みの録画（41・45〜50）の残差のうち、いちばん大きい既知の分を説明できる。
- ボスの出来事と仕組み（狙えない区間）は同じなので、あとでボスのプリセットに同じ型で足せる。

### 9.2 型

```ts
// core/src/enemies.ts（仮）
type EnemyEventKind = 'untargetable' | 'invulnerable' | 'barrier';
type EnemyEvent = { kind: EnemyEventKind; start: number; end: number }; // 秒。[start, end)
```

| 種類           | 意味                                           | 16-B での扱い                                                      |
| -------------- | ---------------------------------------------- | ------------------------------------------------------------------ |
| `untargetable` | 狙えない（射撃場の的の停止・ジャンプ・画面外） | **実装**。9.3 の挙動（確定したものだけ）                           |
| `invulnerable` | 撃てるがダメージが通らない                     | 型とタイムラインの表示だけ。数値には効かせず「未実装」の注記を出す |
| `barrier`      | ダメージを吸う盾（量がある）                   | 同上。量（HP）を持つ形は、バリアのあるボスを撮るときに決める       |

- `TeamInput.enemy`（`EnemyInput`）に `events?: EnemyEvent[]` を足す。省略・空なら**今と 1 フレームも違わない**（全テストがそのまま通る）ことを完了条件の 1 つにする。
- プリセット（`data/enemies.json`）には周期で書く: `{ kind, first, duration, every?: [間隔…], until? }` を秒で持ち、読み込み時に `EnemyEvent[]` に展開する。射撃場の 5 属性で同じなので、トップレベルの `eventSets` に 1 回だけ書き、プリセットは id で参照する。

### 9.3 狙えない間の挙動と、その決め方

共通のフレームループ（`frame/firstPass.ts`・`frame/shooter.ts`）に入れる。**calc と sim の両方に効く**（7 節 4）。

| 項目                          | いまの材料                                   | 決め方                                                   |
| ----------------------------- | -------------------------------------------- | -------------------------------------------------------- |
| 射撃                          | 全員撃たない（録画 41）                      | **確定**。窓の間、射手は撃たない（残弾・位相は止まる）   |
| MG のスピンアップ             | やり直す（録画 41）                          | **確定**。`rateOfFireResetTime` 以上撃たなければ最初から |
| 停止中のリロード（AI）        | 5 回中 2 回（規則不明）                      | 既存の録画 45（MG 単騎）・46（RL 単騎）で読む            |
| チャージ武器（SR・RL）        | なし                                         | 既存の録画 46（RL 単騎）・47（SR+SR+RL）で読む           |
| ゲージ                        | 撃たないので溜まらない（自然にそうなる）     | 仕組みから確定（射撃起点のゲージしか無い）               |
| バーストの発動・FB の時間・CT | なし                                         | 既存の録画 47・49・50 で、停止と FB が重なった回を探す   |
| 持続バフの時間                | なし（時間で切れるはずで、止まる理由が無い） | 仕組みのまま（止めない）。反証が出たら直す               |
| 停止の時刻（位相・間隔）      | 9.0 の表。位相は録画ごとに変わる             | 既存の録画で「最初の停止」を戦闘時計で読み、代表値を置く |

- **まず既存の録画（41・45〜50）で読み、足りない点だけ単騎で撮る**（機構の確定は単騎・最小構成の録画だけで行う。48〜50 の 5 体編成は時刻の裏取りにだけ使う）。
- 読めなかった項目は仮の既定（リロードしない・チャージは保持など）を名前付き定数に置き、未対応・近似の一覧に出す。

撮り足す場合の案（既存の録画で決まらなかったときだけ）:

1. **MG 単騎（クラウン）・3 分モード・180 秒**: 停止の時刻・長さ・停止中のリロード・スピンアップのやり直し（`tools/captures/ammo.ts` で読む）。
2. **SR 単騎（デルタ、操作）・3 分モード・180 秒**: チャージ中に止まったときの扱い。操作キャラも撃てなくなるか。

### 9.4 画面

- 敵の欄に「3 分モード（的の停止）」のチェックを足す（射撃場のプリセットのときだけ出す）。**既定は OFF**（公開サイトの射撃場の数値は今と同じ。通常モードの射撃場には停止が無いため）。
- **sim 表示にタイムライン**を出す: 横軸 = 秒、縦軸 = 1 秒ごとの編成のダメージ（棒）。出来事の区間とフルバーストの区間を帯で重ねる。SVG の手書き（図のライブラリは足さない）。
- calc 表示には、出来事の一覧（種類・区間）と未実装の注記だけ出す。

### 9.5 段取り（1 PR）

1. 既存の録画の読み取り（9.3 の表の「既存の録画で読む」行）→ 結果を 9.0 に追記し、挙動と停止の時刻の代表値を決める。
2. 型・プリセット・共通のフレームループ・画面。数値は、出来事なしでは今と同じ、ありでは 9.3 の挙動どおりであることをテストに固定する。
3. 確かめ: 録画 46・47 の合計の差（+8.7% / +6.1%）が、停止を入れた sim でどこまで縮むかを verification.md に記録する（係数を合わせに行かない。差が残ればそのまま残す）。

### 9.6 決めてほしいこと（2026-09-26 に 6 点とも推奨案で承認）

1. **最初に入れる出来事** — 推奨: **射撃場 3 分モードの的の停止**（9.1）。材料がなくボスから始める案は、撮影が先になる。
2. **種類の範囲** — 推奨: **`untargetable` だけ実装、`invulnerable`・`barrier` は型と表示だけ**（9.2）。
3. **挙動の決め方** — 推奨: **既存の録画で先に読み、足りない点だけ単騎で撮る**（9.3）。
4. **停止の時刻** — 推奨: **プリセットに代表値（最初の時刻・長さ・間隔）を置き、画面では ON/OFF だけ**。位相は録画ごとに変わるので、合計には ±1 回分（約 1〜2%）のずれが残る、と注記する。別案は「最初の停止の時刻」を画面で上書きできるようにする。
5. **既定** — 推奨: **OFF**（9.4）。
6. **タイムラインの形** — 推奨: **1 秒ごとの編成のダメージの棒 ＋ 出来事・FB の帯（sim 表示だけ）**。枠ごとに色分けした積み上げにする案もある。

### 9.7 実装（2026-09-26）

9.0.1 のユーザーの説明で、9.3 の表の「既存の録画で読む」行は仕組みが決まった。録画の読み取りは、停止の時刻と長さの代表値だけにした。

| 項目                                     | 実装                                                                                                                                                                                                                                                       | 根拠                                                                                                               |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 窓の間                                   | 全員撃たない（`stepShooter` の `blocked`）。待ち・リロードは進む                                                                                                                                                                                           | 9.0.1                                                                                                              |
| 窓に入る                                 | 撃てる状態で残弾が減っていれば、ハイド中のリロードを始める（`hideShooter`。チャージ中の分は捨てる）。弾切れのリロード中・満タン・装弾数無限はそのまま                                                                                                      | 9.0.1                                                                                                              |
| 窓が明ける                               | ハイド中のリロードが終わっていなければ取り消す（残弾は減らない。分割リロードで込め終えた分は残る）。撃てる状態なら**明けのフレームに撃ち直す**。窓が `rateOfFireResetTime` 以上なら MG のレートは最初から。チャージ武器はチャージし直す（`unhideShooter`） | 録画 41・48・49 の最後の射撃 → 撃ち直しが 111〜126f（窓 120f とほぼ同じ）。録画 41 のクラウンの間隔 23・13・10・8… |
| オートバースト                           | 窓の間は発動しない（`stepBurstController` の `blocked`）。満タン・フルバーストの終わりは進み、明けたフレームに発動する                                                                                                                                     | 9.0.1                                                                                                              |
| ゲージ・持続バフ・CT・フルバーストの時間 | 変えない（射撃が無いのでゲージは溜まらない。時間はそのまま進む）                                                                                                                                                                                           | 仕組み                                                                                                             |
| 停止の時刻                               | 出来事のセット `range-3min-jump`（`data/enemies.json`）: 最初 31 秒・長さ 2 秒・36.4 秒ごと（180 秒で 5 回）                                                                                                                                               | 9.0 の表の中央値。最初のジャンプは 25.7〜32.7 秒、間隔の中央値 2,186f                                              |
| calc                                     | 狙えない窓があるときは、全グループで射撃の列を数える（平均レートでは撃ち直し・ハイド中のリロードを表せないため）                                                                                                                                           | 7 節 4                                                                                                             |
| 距離                                     | 着地後の距離の変化（距離ボーナス）は扱わない。注記に出す                                                                                                                                                                                                   | 9.0.1。距離ボーナスは枠ごとの固定の条件のまま                                                                      |

設計からの差分と、決めたこと:

- **出来事があると calc と sim は一致する**（どちらも 1 パス目の射撃の列を数えるので、離散化の差も消える）。出来事が無いときは今までどおり。
- 敵の出来事は `EnemyInput.events`（秒）で渡し、共通のフレームループには `frame/events.ts` の `untargetableRanges` でフレームの窓にして渡す。結果（`TeamResult`）には入力の出来事と注記（`enemyNotes`）、sim だけ 1 秒ごとのダメージ（`damagePerSecond`）を足した。
- 画面の ON/OFF は編成の保存形式に入れた（`enemyEventSets`、欠落は OFF）。効くのは、敵の値がそのセットを持つプリセットと一致しているときだけ（値を手で変えるとカスタム扱いで出来事なし）。
- CLI: `npm run sim -- --enemy range-bigarms-fire --events range-3min-jump`。
- 着地までの移動で距離が変わる点は、距離ボーナスを枠ごとの固定の条件で持っている今の形では入れられない。時刻で変わる条件にするのは、距離を測る撮影をしてから。

確かめ（9.5 の 3。係数は合わせに行っていない）:

| 録画                                  | 実機        | sim（停止なし）      | sim（停止あり）           |
| ------------------------------------- | ----------- | -------------------- | ------------------------- |
| 46（紅蓮BS 単騎・3 分モード）         | 79,851,043  | 86,815,757（+8.7%）  | 79,774,655（**−0.10%**）  |
| 47 の紅蓮BS（ラム + デルタ + 紅蓮BS） | 284,726,954 | 302,004,698（+6.1%） | 284,522,650（**−0.07%**） |

15-A（録画 48〜50）は 5 体の編成なので、この節では比べていない（15-A の残差は単騎・最小構成の切り分けの後に扱う、という 2026-09-25 のユーザー決定のとおり）。
