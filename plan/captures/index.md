# 録画台帳

- 関連: [../verification.md](../verification.md)、[../../tools/captures/](../../tools/captures/)
- 目的: 録画 1 本ごとに「誰を・どの条件で撮ったか」と「その録画がどの実測値の根拠か」を固定する。動画と証拠フレームは Git に入らない（公開リポジトリ）。この台帳が録画と実測値を結ぶ唯一の追跡可能な記録で、証拠フレームは録画と同じ E: に置く。

## 置き場所

録画は容量が読めないので D: のリポジトリには置かず、**E:（2.8TB HDD）に集約する**。証拠フレームも E: に置き、公開しない（2026-09-26 から）。リポジトリ側に残すのは台帳と手引きだけ。

```
H:/record/                      取り込み元: 撮った録画・スクショの保存先（元ファイル名のまま。2026-09-26 に H:/video から作り直した）
E:/record/nikke/                取り込み元: 未分類の生録画（既存アーカイブ。元ファイル名のまま）
  動画/                         269 本・約 110GB
  命中/ リザルト/ …              過去の検証素材

E:/nikke_project_captures/      このプロジェクトが採用した録画（種別ごとに分ける）
  range/                        射撃場（1 ヒット・発射サイクル。Stage 15-A の 3 分モード・スペック固定 OFF もここ）
  interception/                 迎撃戦（異常個体 / 特殊個体）の実戦（Stage 15-B）
  raid/                         ソロ / ユニオンレイドの実戦
  skill/                        スキル発動・バフ持続
  burst/                        バーストゲージ・フルバースト
  ui/                           ステータス画面・ShiftyPad などの画面録画と、録画に付かないスクショ
  frames/*.jpg                  証拠フレーム（台帳の `frames/…` はここを指す）
  reference/                    録画ではない参照資料（画像）。下の「参照資料」
    tooltips/                   ゲーム内の用語のツールチップ（ファイル名は用語）
    prerelease/                 実装前に先行公開されたスキル画像

D:/nikke_project/plan/captures/ リポジトリ側（Git 追跡）
  index.md                      この台帳
  guide.md                      撮影と読み取りの手引き
```

種別フォルダは必要になった時点で作る（現状あるのは `range/`・`interception/`・`frames/`・`reference/`）。**種別の語彙はこの一覧に足してから使う。** 録画の種別は `records/recordings/<録画 id>.json` の `folder` と同じ語彙（`packages/core/src/records/recordings.ts` の `RECORDING_FOLDERS`）。

### 運用

取り込み元は `H:/record/` と `E:/record/nikke/` の 2 つ。撮った録画とスクショはどちらかに置かれる。

1. 撮る → 取り込み元に保存される。読んでいないものはそのまま置いておく。
2. 読み込んだ（解析に使った）録画とスクショは、検証に採用したかどうかにかかわらず `E:/nikke_project_captures/<種別>/` へ**移し**、規約名を付ける。取り込み元には残さない（同じ録画が 2 箇所にあって食い違う状態を作らない）。
   - ドライブをまたぐので、コピー → sha256 の一致を確かめる → 取り込み元から消す、の順に行う。
   - スクショは、関連する録画と同じ種別フォルダに置く。録画に付かないスクショ（ステータス画面だけ など）は `ui/` に置く（名前は下の「命名規約」）。
   - 例外: 旧プロジェクトの録画（`L-` の番号）は移さない（下の「旧プロジェクトの録画」）。
3. 録画は `records/recordings/<録画 id>.json` を 1 本足し、`npm run records:table` で録画の一覧（[recordings.md](recordings.md)）を作り直す。採用しなかった録画にも通し番号を振って載せ、採用しなかった理由を `conditionNote` に書く。スクショは `records/recordings/` に載せない（録画に付くものはファイル名の番号で結びつく。録画に付かないものは、それを読んだ記録（verification.md など）にファイル名を書く）。証拠フレームは `E:/nikke_project_captures/frames/` に切り出す（コミットしない）。
4. Google Drive に同期する（下記）。移した録画・スクショ・証拠フレームはどれも `E:/nikke_project_captures/` の下にあるので、フォルダ全体を同期すれば全部バックアップされる。

### 参照資料（`reference/`）

録画ではないので台帳の番号は振らない。公開しない（出典はゲーム内の画面と公式の告知画像）。

- `tooltips/`: スキル情報の「」内の用語を押したときに出る説明（例: 分配ダメージ・フォーカス・デコイ）。100 枚、2026-08-09 撮影（旧プロジェクトの時期）。ファイル名は用語そのもの（日本語）。
- `prerelease/`: 実装前に先行公開されたスキル紹介画像。数値はスキル Lv10 の値で、実装時に変わることがある（画像内の注記）。名前は `<rid>_<英名>_<スキル>.jpg`。

| ファイル                               | キャラ（rid）                   | 内容               | 元のファイル名        | 取得日     |
| -------------------------------------- | ------------------------------- | ------------------ | --------------------- | ---------- |
| `404_guilty-mighty-bunny_s1.jpg`       | ギルティ：マイティバニー（404） | スキル 1           | `HSUWN_AbMAAji9Q.jpg` | 2026-09-16 |
| `404_guilty-mighty-bunny_s2-burst.jpg` | ギルティ：マイティバニー（404） | スキル 2・バースト | `HSUWN-3bcAAZEb8.jpg` | 2026-09-16 |
| `405_sin-swift-bunny_s1.jpg`           | シン：スウィフトバニー（405）   | スキル 1           | `HSUldGBbIAAH7K4.jpg` | 2026-09-16 |
| `405_sin-swift-bunny_s2-burst.jpg`     | シン：スウィフトバニー（405）   | スキル 2・バースト | `HSUldDwbUAA9ohl.jpg` | 2026-09-16 |

## バックアップ

`E:/nikke_project_captures/` を Google Drive にバックアップする。Google Drive for desktop がストリーミングモードで 2 アカウント分マウントされている。**使うのはメインアカウント側の `I:`**（別アカウントの `J:` は使わない）。

```bash
robocopy "E:/nikke_project_captures" "I:/マイドライブ/nikke_project_captures" /E /R:1 /W:1
```

- `/E` はサブディレクトリを含めて差分コピーする。コピー元で消したファイルはコピー先に残る（`/MIR` のようにミスが伝播しない）。リネームすると**コピー先に旧名が残る**ので、必要なら手で消す。
- **Git Bash から実行しない。** `/E` がパス `E:/` に変換されて「無効なパラメーター」になる。PowerShell か cmd から実行する（パスも `\` 区切りで渡す）。
- 録画・スクショを足したタイミングで実行する。
- robocopy の終了コードは 0〜7 が正常（1 = コピーした、0 = 差分なし）。8 以上が失敗。

同期したら sha256 で中身を突き合わせる。`probe.ts` はどのディレクトリにも使える:

```bash
node tools/captures/probe.ts "I:/マイドライブ/nikke_project_captures"
```

`probe.ts` が見るのは `.mp4` だけなので、スクショと証拠フレームは PowerShell の `Get-FileHash`（既定で SHA256）で突き合わせる。

同期の実行の記録は [backup-log.md](backup-log.md) にある（同期したら 1 段落足す）。

### 冗長性の現状と限界

| 対象               | 冗長                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------------------- |
| 台帳               | Git → GitHub（`HKHKM-dev/nikke_project`）                                                         |
| 録画・証拠フレーム | E: の実体 + Google Drive（`I:`）。**別拠点にコピーがある**                                        |
| 取り込み元         | `H:/record/`・`E:/record/nikke/` にあるのは未読のものだけ（読んだものは移す）。バックアップ対象外 |

**注意: `I:` のローカル表示の空きは Drive 本体の容量ではなく、Google Drive デスクトップアプリのローカルキャッシュ置き場の空きに連動する。** 表示が少なくなってきたら、まずキャッシュドライブの設定を疑うこと。

## 命名規約

```
<YYYYMMDD>-<番号 3 桁>_<種別内の識別子>[_<補足>].<拡張子>
range の例: 20261001-064_AR_kurumi.mp4     （武器種_測定対象のキャラ英名）
raid の例:  20261001-065_solo_5team-a.mp4  （モード_編成名）
interception の例: 20261001-066_special_boss-name.mp4 （種類_ボス英名）
証拠フレーム: 064_f1200_ammo.jpg          （番号_f<フレーム>_<内容>）
スクショ（録画に付く）: 20261001-064_AR_kurumi_result.png （録画と同じ名前_<内容>）
スクショ（録画に付かない）: ui/20261001_status_kurumi.png （日付_<内容>。通し番号は取らない）
```

- 番号は**全録画の通し番号**（種別をまたぐ）。2026-09-26 に 063 まで使った。**064 から 3 桁**にする（Stage 19。[../design-stage19.md](../design-stage19.md) 2.6 節）。`records/recordings/<録画 id>.json` の `id`（ファイル名）と同じ値にする。
- **編成はファイル名に入れない**。編成と条件は `records/recordings/<録画 id>.json` に書く（ファイル名が長いと Windows のパス長の上限にかかる）。識別子には測定対象を 1 体だけ書く。
- 既存の録画（01〜63）と証拠フレームは改名しない（2 桁の番号と編成入りの名前のまま。Drive 側に旧名が残るのを避ける）。
- キャラ名は `packages/core/data/characters/<rid>.json` の `name.en` を小文字ケバブ化する。
- 日本語・空白は使わない（ffmpeg にそのまま渡せるように）。
- 条件違いの撮り直しだけ `_dist-out`（適正距離外）のような補足を付ける。
- 元のファイル名は必ず `records/recordings/<録画 id>.json` の `original` に残す。リネームは可逆。

## 撮影プロトコル（次回以降）

読み取り時の条件の取り違えを防ぐため、**録画の中に条件が写っている状態**にする。

1. 撮影前に、そのニケの部隊画面（攻撃力の表示値が見える状態）を 2〜3 秒撮る。
2. 射撃場に入ったら的の名前（`BigArms` 等）と属性が読めるフレームを含める。
3. 編成はそのニケ 1 体のみ。オートバースト OFF、カバーなし。
4. 戦闘開始から最初のリロード完了後の 1 発目まで（15 秒以上）を含める。
5. **録画を止める前に、キャラ名が文字で出る画面を 2〜3 秒映す。** 戦闘中に ESC を押した「一時停止」か、最後まで戦った後の「戦闘履歴」。どちらも枠順・クラス・**キャラ名**・LV・戦闘力・与ダメージ総計が並ぶ（レイアウトは同じ）。
6. 撮り終えたら即 `records/recordings/<録画 id>.json` を 1 本足す。**後からまとめて埋めない。**

実戦（迎撃戦・レイド。Stage 15）では、1〜5 に加えて次を録画に含める（plan/design-stage12.md 5.3 節）: 出撃前の編成画面（各ニケの戦闘力）、ボスの名前・レベル・属性が読めるフレーム、**リザルト画面（キャラ別ダメージ・被弾回数）を数秒**。撃破で 180 秒より短く終わったときは撃破時刻を台帳に書く。同じ編成 × 同じボスの 3 回を 1 日で撮る（迎撃戦は 1 日 3 回）。

5 は台帳を書く前の裏取りに直結するので飛ばさない。**射撃場の出撃画面のカードには名前が出ない**（クラス・属性・レア度・Lv だけ）ので代わりにならない。長い名前は名前欄が横スクロールして切れる（録画 21 のクイーン（真）は「ーン（真）」しか写らないフレームがある）ため、**一瞬ではなく数秒映す**。

スペック固定で撮る場合、攻撃力は `computeFixedSpecAttack` で決まるので 1 は省略できる。代わりに出撃画面（属性タブとスペック固定 ON の表示）と、戦闘後の一時停止画面（編成と Lv.400）を録画に含める（録画 10〜14 はこの形）。

## 台帳

### 録画の一覧

表は [recordings.md](recordings.md) にある（`records/recordings/<録画 id>.json` から `npm run records:table` で生成する。手で書かない。条件を直すときは JSON を直して作り直す）。以下は録画ごとの注記。

01〜35 はすべて `E:/nikke_project_captures/range/` にある。**39・40 は 2026-09-23 に H:/video から移動した**（Stage 10 の録画 A・B）。**41 も同日に移動した**（Stage 11 のクラウン。sha256 は移動の前後で一致）。**23〜35 は 2026-09-23 に H:/video からコピーした**（元ファイルは H:/video に残っている。sha256 は一致）。 **46・47 は 2026-09-24 に H:/video から移動した**（紅蓮：ブラックシャドウの先行撮影。sha256 は移動の前後で一致）。44・45 はモダニア編の録画で、同じ日にモダニア編のブランチで登録している。**48〜50 は 2026-09-25 にデスクトップ（`20260924/`）から移動した**（Stage 15-A。sha256 は移動の前後で一致）。**スペック固定 OFF の最初の録画**で、名前の末尾の `_spec-off` で 47 本までと区別する。**51〜53 は 2026-09-25 に H:/video からコピーした**（Stage 15-B。迎撃戦（特殊個体）の最初の録画。元ファイルは H:/video に残している。sha256 は一致）。種別フォルダ `interception/` に配置。

**54〜56 は 2026-09-26 に H:/video から移動した**（Stage 17。sha256 は移動の前後で一致）。単騎なので操作キャラだが、AUTO 射撃 ON で人は狙っていない（ユーザー）。I-DOLL を操作して撃たせない形（23〜35 の AI）とは枠の扱いが違うが、MG ではエマ AI（35）のコア 97.0% とクラウン AUTO（56）の 96.3% が同じ程度だった。

23〜35 は Stage 7 のゲージ較正で、同じキャラを「操作してコアを狙う / 操作して胴体を撃つ / AI にする（I-DOLL を操作して撃たせない）」の 3 通りで撮った単騎の録画（ノワールは SG なのでコアだけは狙えず、操作と AI の 2 本。ベロータは操作の 2 本）。読み取りは `tools/captures/gauge.ts`、結果は [../verification.md](../verification.md) Stage 7 節。**フレーム数は ffprobe が返さない**（コンテナに nb_frames が入っていない）ので「—」にした。

01〜22 は `E:/nikke_project_captures/range/` にある。01〜09 の的は射撃場の中型ラプチャー `BigArms`（風属性・防御 100 相当）で、9 本とも同一個体（[verification.md](../verification.md) のユーザー確認）。攻撃側が炎のエマだけ属性有利 ×1.1 が乗る。

10〜15 は **灼熱タブ**の `BigArms`（灼熱属性、WEAK POINT 水冷、防御 100 相当）で、スペック固定 ON。攻撃側は風圧か灼熱なので属性有利は誰にも乗らない。11 と 13 は 2 体編成（定義のない味方を受け手にして味方効果を切り出す）。15 は 4 体編成（FB 補正の切り出し）。読み取り方法と予測との突き合わせは [verification.md](../verification.md) Stage 4・Stage 5 節。

18〜22 は Stage 6（バースト時トリガーの持続バフ）と Stage 5 の持ち越しの実測で、**射撃場の 03:00 モード**（3 分・的が反撃する）で撮った。18 と 20 は **電撃タブ**、19・21・22 は灼熱タブで、いずれも測定対象に属性有利は乗らない。18〜21 は「エーテル（I）+ デルタ（II）+ 測定対象（III）」、22 は III を入れずにフルバーストを起こさない編成。

**キャラの同定はカードの見た目で行わない。** 21 は出撃画面のカードから一度モダニアと読み違えた（クイーン（真）の怪盗衣装は金属マスクでモダニアに似ている）。1 ヒットが 47,172 / 70,758 / 94,344 で録画 12 のクイーン（真）と一致したことで判明した。名前の出るパネルの手順は下の「[誰が写っているかの確かめ方](#誰が写っているかの確かめ方)」にまとめてある。突き合わせは [verification.md](../verification.md) Stage 6 節。

**この 5 本で分かった撮影上の注意**:

- **エーテルを I に使ってはいけない**。S2 が「フルバースト中に発動したとき敵の防御力 9.38%▼（6 秒）」を付けるので、フルバースト中の数値が 2 系列に割れる（録画 18 の 37,510 と 37,512）。次からは I を別のニケにする。
- **3 分モードは的が反撃する**ので、被弾トリガーのスキルが発動する。録画 18 ではラピの S1（N 回被弾で攻撃力 21.81%▲）が 3 回目のバースト以降に乗っていた。**スキルの切り出しに使えるのは 1 回目のフルバーストまで。** さらに**約 36 秒ごとに約 2 秒、的が狙えなくなる**（録画 41。HUD の総ダメージも止まり、編成全員が撃たない。MG はそのあとスピンアップをやり直し、AI はその空き時間にリロードする）。射撃の回数を数える検証では、この区間を除いて読む。
- III が 1 体だと**フルバーストは CT 40 秒周期で 180 秒に 5 回**（録画 19 の動画時刻で約 21 / 60 / 100 / 139 / 179 秒）。2026-09-23 に 4 回から訂正した（5 回目の数え漏れ。証拠は `frames/20260922-19_SG+SR+SG_ether+delta+noir_f10800_fb5.jpg`）。固定 20 秒サイクルのモデル（9 回）との差はここで実測できる。

16・17 は単騎で SG の発射間隔と 3 分割リロードの実時間を較正したもの（Stage 2-B）。どちらも末尾に一時停止画面が写っており、そこでキャラを確定した。

**16 のキャラはドレイク（rid 101・灼熱・Attacker）で、ドレイク：グレイトヴィラン（rid 104・風圧・Defender）ではない。** 2026-09-23 に `probe-result.ts` で切り出した一時停止画面で判明した（名前欄が「ドレイク」のまま切れずに収まっており、属性アイコンが赤い炎）。実測した 3 分割リロード 30f×3 = 90f も rid 101 の `reloadTime` 0.5 秒・`reloadBullet` 0.33 と一致する（rid 104 は `reloadTime` 1.5 秒・`reloadBullet` 1 で分割リロードにならない）。**2026-09-23 に動画と証拠フレームを `20260922-16_SG_drake` にリネームした**（内容は変えていないので sha256 は `4093b7a9d390` のまま）。

### 旧プロジェクトの録画（参照のみ。移動しない）

旧プロジェクト（`D:\NIKKE_Damage_Calculator`）で撮った録画を、Stage 18-A の検算のために、このリポジトリの読み方で読み直した。

- 旧の資料が元のパスで参照しているので、**`E:/record/nikke/` に置いたままにし、移動もコピーもしない**（design-stage18.md 2 節。上の運用 2 の例外。2026-09-26 に取り込み元を 2 つにして「読んだものは移す」にしたときも、オーナーの判断で例外のまま残した）。
- 番号は旧のタグに `L-` を付けて呼ぶ（`L-AD` など）。キャラと条件は旧の台帳（旧 `docs/動画計測.md`）のまま。
- 読み方: `fps=60,crop=190:38:865:34` で HUD の総ダメージを切り出し、Stage 17 と同じ型の当てはめで読んだ。HUD の最後の値は、5 本とも旧の戦闘結果と 1 の位まで一致した。
- どれも射撃場・3 分モード・スペック固定 ON・灼熱の的・単騎・完全放置（AUTO）。

表は [recordings.md](recordings.md) の「旧プロジェクトの録画」。

### 素性（`node tools/captures/probe.ts` の出力）

表は [recordings.md](recordings.md) の「素性」。

いずれも 1920×1080 / AV1。**01・07・10 は平均 fps が 60 を大きく下回り、11 も少し下回る**（フレーム落ち）。この 4 本は時間計測の母数に使わない（1 ヒットの読み取りには影響しない）。07 は ffmpeg のデコードでもパケットエラーが出る。

### 用途と証拠（2026-09-26 までの旧台帳の記述）

[legacy-usage.md](legacy-usage.md) に移した（2026-09-27、Stage 20-C。凍結して書き換えない）。

## 誰が写っているかの確かめ方

見た目（立ち絵・ポートレート・出撃画面のカード）でのキャラ同定は当てにならない。実際に 2 回取り違えている（下記）。**名前が文字で出ているフレームを証拠に取る。**

### 1. 名前の出るパネルを切り出す

```bash
node tools/captures/probe-result.ts <動画...> --list            # 区間を探すだけ
node tools/captures/probe-result.ts <動画...> --out-dir E:/nikke_project_captures/frames
```

「一時停止」（戦闘中に ESC）と「戦闘履歴」（戦闘終了後）は同じレイアウトで、枠順・クラス・**キャラ名**・LV・戦闘力・与ダメージ総計が並ぶ。画面上の位置もほぼ固定なので、決め打ちのクロップで切り出せる。

検出は「中央のカードが明るく周囲が暗い」×「カード上部に青いヘッダー帯がある」の AND で、青ヘッダーの有無が射撃場の出撃画面との区別に効く（**パネル 60〜87% に対し出撃画面・戦闘中は 0〜24%** と離れている）。切り出した画像の文字は OCR せず目視で読む。

名前欄は長いと横スクロールして切れるので、1 区間から複数枚サンプルする（`--samples`、既定 3）。録画 21 のクイーン（真）は「ーン（真）」、録画 22 のエマ：タクティカルアップグレードは「：タクティカル」しか写らないフレームがある。

**2026-09-23 に全 22 本へ適用し、22 本とも区間がちょうど 1 つ検出され（誤検出ゼロ）、読み取った名前は台帳の「キャラ」列と全件一致した。** 撮影プロトコルに入れる前の録画（01〜09）も末尾に一時停止画面が写っており、遡って確かめられる。

### 2. 弾数カウンターで武器種を確定する（補助）

1. 戦闘開始直後のフレームで、照準の左にある弾数カウンターを読む（位置は照準に追従するので録画ごとに違う）。装弾数で武器種がほぼ一意に決まる: MG 300 / SMG 120 / AR 60 / SG 9 前後 / SR・RL 6。
2. 武器種が決まれば、`packages/core/data/characters/<rid>.json` の `shot.damage`（÷10000 が武器倍率）と `element` から 1 ヒットの理論値が出る。

| 録画 | フレーム | 弾数 | 判定                | 証拠                                         |
| ---- | -------- | ---- | ------------------- | -------------------------------------------- |
| 03   | 800      | 298  | MG（300 発）＝エマ  | `frames/20260922-03_MG_emma_f800_ammo.jpg`   |
| 04   | 800      | 058  | AR（60 発）＝クルミ | `frames/20260922-04_AR_kurumi_f800_ammo.jpg` |

### 過去の取り違え

- 03 のエマ（黒い軍服にベレー帽）と 04 のクルミ（金髪でスーツケース型の銃とドローン）を立ち絵で取り違え、弾数カウンターで訂正した。
- 21 を出撃画面のカードからモダニアと読み違えた（クイーン（真）の怪盗衣装は金属マスクでモダニアに似ている）。1 ヒットが録画 12 と一致したことで判明した。

## フレーム番号の約束

**フレーム番号は「デコードされたフレームの通し番号（0 始まり）」とする。** ffmpeg では `-fps_mode vfr` が相当する。`tools/captures/` のツールはすべてこの番号で統一している。

`-fps_mode cfr` で 60fps に引き伸ばすと重複フレームが挿入され、フレーム落ちのある録画（01・07）では番号がずれる。2026-09-22 の較正メモに残っている番号（SR の 551→633→715 など）は別の数え方で取られたもので、現在のツールの出力とは直接比較できない。**間隔（82f など）は一致するので、較正結果そのものは有効。**

## 解析ツール

```bash
node tools/captures/probe.ts                        # 全録画の素性を表で出力（records/recordings/ の素性の項目に写す）
node tools/captures/still.ts   <動画> --frame 947 --out out.png [--crop x,y,w,h] [--scale 900]
node tools/captures/diff.ts    <動画> --crop x,y,w,h [--from N] [--to N] --peaks [--csv out.csv]
node tools/captures/probe-result.ts <動画...> [--list] [--out-dir DIR] [--samples 3]
node tools/captures/gauge.ts <動画> [--from N] [--to N] [--mode events|jumps|series] [--step 1]
node tools/captures/ammo.ts  <動画> [--crop 785,902,110,26] [--max 300] [--mode mags|series] [--from N] [--to N]
node tools/captures/hud.ts   <動画> [--mode final|jumps|series] [--from N] [--to N] [--crop 810,34,300,38]
node tools/captures/aim.ts   <動画> [--from N] [--to N] [--step 60] [--csv out.csv] [--debug-dir DIR]
```

`gauge.ts` は画面右の BURST バー（x 1793〜1905・y 442）の充填率を 1 フレームずつ読む（Stage 7 のゲージ較正用）。`events` は溜め始め・満タン・バー消失、`jumps` は 1 フレームで跳ねた増分（SR / SG の 1 発ずつ）、`series` は充填率の列。フルバースト中・CT 待ち・チェーン中はバーの位置に別の UI が出るので読めない（`-`）。**ゲージの較正を撮るときは、誰を操作しているか（照準画面が出ているニケ）を台帳に書く**。操作キャラと AI でゲージ量が違うらしい（[../verification.md](../verification.md) Stage 7 節）。

`ammo.ts` は枠アイコンの上の残弾表示「残弾/最大」を 1 フレームずつ読む（MG の射撃レートの再較正用、[../design-mg-fire-rate.md](../design-mg-fire-rate.md)）。**AI の味方の射撃フレームはこの表示からしか読めない。** 表示は中央揃えのプロポーショナルフォントなので、「/最大」の位置を先に探してから桁をテンプレート（`ammo-templates.json`、録画 41 の暗い背景のフレームから作成）と照合し、「残弾は増えない・リロード明けは最大」の制約で復号する。`mags` はマガジンごとの 1 発目・最終弾・次の最大の表示と 4f 以上止まった区間、`series` は残弾の列。`--crop` の既定は 4 人編成の 2 枠目・2 人編成の 1 枠目（枠の間隔は約 130px）。分割リロードの武器には使えない。録画 41 の全 12,573 フレームで約 30 秒。

`hud.ts` は画面上部中央の HUD の総ダメージを全フレーム読む（Stage 19-D）。`final` は最後の値（単騎なら戦闘履歴の与ダメージと同じ）、`jumps` は増えたフレームと増分（単騎で 1 フレームに 1 ヒットなら 1 ヒットの値）。数字は見本（`hud-templates.json`、録画 46 の 5 フレームから作った）と照合し、総ダメージは減らないので最長の非減少列から外れた読み（0 と 8 の読み違い・先頭の桁の欠け）を落とす。2026-09-26 に録画 10・46・47 と旧の 5 本で、最終値と 1 ヒットの値が記録と一致することを確かめた（[../verification.md](../verification.md) Stage 19-D 節）。

`aim.ts` は照準・的・コアの位置と大きさを、`--step` フレームごとに CSV で出す（Stage 18-B2 の試作、[../design-stage18.md](../design-stage18.md) 11.6 節）。

- 照準の中心は、画面の端から端まで通る細い横線・縦線から粗く求め、十字の腕（白・水色・黄）かリングを当てはめて出し直す。精度は ±2px 以内。
- 十字の隙間やリングの半径（`aim_size`）は撃っている間に広がるが、照準円（半透明の円）の大きさではない。照準円は的の上ではほぼ見えないので、空に向けた録画で測る（[guide.md](guide.md)「照準円」）。
- 的は、戦闘中のフレームの中央値を背景として、暗い画素の外接矩形を取る。爆発・遮蔽物・カットインで外れるので、区間ごとの中央値で使う。
- **コアの検出は試作の段階で、まだ信頼できない**（エフェクトや赤い所を拾う）。

`probe-result.ts` はキャラ同定用（上の「誰が写っているかの確かめ方」）。複数の動画をまとめて渡せる（`--list` なら区間を出すだけで画像は書かない）。判定は `--step` フレームおきなので区間の端は ±`--step` の誤差がある。

`diff.ts` は指定領域のフレーム間差分を取り、閾値を超えた局所ピークをフレーム番号と間隔で返す。領域と閾値は録画ごとに調整が要る。2026-09-22 の較正で有効だった領域（1920×1080 での `x,y,w,h`）:

| 狙うもの               | 領域の目安        | 備考                                             |
| ---------------------- | ----------------- | ------------------------------------------------ |
| SR/RL の発射（反動）   | `300,780,700,280` | 手元。`--sigma 2 --min-gap 20`                   |
| AR の発射（反動）      | `760,620,360,300` | 同じ手元のパルス列。`--sigma 0.2 --min-gap 4`    |
| MG/SMG の命中          | `820,10,280,50`   | HUD 総ダメージカウンター。1 ヒットごとに更新     |
| ダメージ数値の読み取り | `940,220,400,180` | 的の周りに出る数値。`still.ts --crop` で切り出す |

照準の横の残弾カウンター（操作キャラ）は数字がロール表示されて毎フレーム変化するので検出には使えない。枠アイコンの上の残弾表示は読める（`ammo.ts`）。

1 ヒットの値を読むときは、HUD 総ダメージカウンターを 1 フレームごとに並べて（`select='between(n,A,B)',crop=280:50:820:10,tile=6x30`）差分を取るのが速い。2 体編成では同じフレームに 2 人分が乗ることがある（録画 13 の 930,273 = 686,756 + 243,517）ので、単独で増えたフレームを探す。

## 証拠フレームの切り出し

証拠フレームは `E:/nikke_project_captures/frames/` に置き、公開しない（2026-09-26 にリポジトリから外した）。台帳などの `frames/…` はこのフォルダを指す。録画と一緒に Google Drive へ同期する。

各録画から 2 枚（全体像の 70% / 85% 地点）＋数値の拡大を置いてある。全体像は次のコマンドで作った。

```bash
ffmpeg -v error -i <動画> -vf "select='eq(n,<フレーム>)',crop=1260:420:340:0,scale=900:-1" \
  -fps_mode vfr -frames:v 1 -q:v 3 -y frames/<名前>_f<フレーム>.jpg
```

**証拠フレームには検証に使う部分だけを入れる。**
