# 検証記録

- 関連: [design-stage1-2.md](design-stage1-2.md)、[roadmap.md](roadmap.md)、[captures/index.md](captures/index.md)（録画台帳。どの録画がどの実測値の根拠かはここで引く）
- 目的: 計算値と実測値の突き合わせ結果と、そこから較正した定数を記録する。
- 方針（2026-09-22 決定）: 「90 秒の総ダメージ」で合わせるのはやめ、式の要素ごとに実測しやすい量で検証する。
  - 攻撃力は Blablalink（ShiftyPad）の表示値を正とする。ゲーム内表示は上乗せ分があるので絶対値では比較しない。
  - ダメージ式は射撃場の 1 ヒット数値で検証する。時間や距離のブレ、スキルの影響を受けにくい。
  - 発射サイクルはダメージではなく時間（録画のフレーム数）で測る。測るのは疑わしい SR/RL のチャージと MG のスピンアップだけ。

## Stage 1: 攻撃力（素のステータス）

判定基準: Blablalink の ShiftyPad（ニケ詳細ページのレベル / 凸 / コアのスライダー）が表示する攻撃力と一致すること。数体分、レアリティ（SSR / SR / R）と凸・コアの組み合わせを変えて確認する。

| キャラ         | resourceId | レア | Lv  | 凸  | コア | 計算値 | ShiftyPad | 判定 |
| -------------- | ---------- | ---- | --- | --- | ---- | ------ | --------- | ---- |
| エマ           | 90         | SSR  | 200 | 3   | 7    | 21307  |           |      |
| エマ           | 90         | SSR  | 200 | 0   | 0    | 17576  |           |      |
| ラピ           | 10         | SR   | 200 | 2   | 0    | 19778  |           |      |
| ソルジャーE.G. | 300        | R    | 200 | 0   | 0    | 16873  |           |      |

**結果: Stage 1 の完了条件を満たした（SSR / SR の両係数で一致）。** R は未確認だが係数の差（+16）以外は同じ式。

2026-09-22 確認（ニケ図鑑のエマ・ラピ詳細ページ、レベルとスペアボディのスライダーで操作。ShiftyPad の表示は「基礎値 + 増分」形式なので合算して比較）。HP と防御力も同時に一致を確認: エマ Lv200/3凸/コア7 で HP 647453、防御力 3894。

注意: ShiftyPad のスライダーは SR でもスペアボディ 10 まで動く（ラピ Lv200 で 23000 = SSR と同じ「3 凸 + コア 7」の式）。ゲーム内では SR は 2 凸まで・コア強化は SSR のみなので、core のレアリティ制限はそのまま維持する。

計算値の出し方（Node 24、リポジトリ直下で実行）:

```bash
node -e "import('./packages/core/src/index.ts').then(m=>{const c=JSON.parse(require('fs').readFileSync('packages/core/data/characters/90.json','utf8'));console.log(m.computeStat(c,'attack',{level:200,grade:3,core:7}))})"
```

任意の補助確認（差分のみ）: 同じニケで凸またはコアを 1 段上げる前後のゲーム内表示攻撃力の増分を記録し、計算上の増分と一致するかを見る。上乗せ分（コンソール・装備・キューブ・好感度）は差分で消える。

| キャラ | 変更内容       | 変更前（ゲーム内） | 変更後（ゲーム内） | 増分 | 計算上の増分 | 判定 |
| ------ | -------------- | ------------------ | ------------------ | ---- | ------------ | ---- |
|        | 例: コア 0 → 1 |                    |                    |      |              |      |

## Stage 2-A: ダメージ式（1 ヒットの実測）

射撃場で通常攻撃 1 ヒットの表示値を読む。敵は雑魚（防御 100）とし、属性相性なし・距離ボーナスは適正距離で撃つ（RL は距離ボーナスなし）。バーストは撃たない。通常攻撃に影響するスキル（自己バフ・攻撃ダメージ増加・弾数変化）を持たないキャラを選ぶ。

期待値の式（1 ヒット、期待値ではなく実値）:

```
基礎     = 攻撃力（ゲーム内表示の実攻撃力。ここは上乗せ込みの値をそのまま使う） − 100
1 ヒット = 基礎 × 武器倍率（SG は 1 ペレット分 = damage / shotCount）
           × (1 + コア命中なら (coreDamageRate − 1) + 会心なら (crit.damage − 1) + 距離ボーナスなら 0.3)
           × 属性有利なら 1.1
           （小数点以下は四捨五入。SR/RL はさらに × fullChargeDamage）
```

### 測定条件（2-A 共通）

- 場所: 射撃場。敵は名前付きの中型ラプチャー「BigArms」（防御 100 相当、下記の突き合わせ結果で確認）。**敵の属性は風圧（Wind）で固定**（一連の撮影はすべて同一個体・同一属性の的に対して行った。ユーザー確認）。「コアの有無」も記録する（コアが無い的ならコア列は空欄）。属性有利になるキャラ（攻撃側が灼熱＝Fire）を使う場合はダメージに ×1.1 の補正が乗るため、逆算時に必ず考慮すること。今後、的を変える場合は防御力・属性を必ず記録する。
- 編成: 測定するニケ 1 体だけを出撃させる（味方バフの混入を防ぐ）。バーストは撃たない（自動バースト OFF）。
- 距離: 適正距離かどうかは、1 発の値が 1.3 倍違うかで判別できるので、判別結果を記録する。
- 撃つ弾: 戦闘開始直後の最初のマガジンだけ読む（「通常攻撃 N 回で自己バフ」型のスキルが乗る前）。SR/RL はフルチャージのみ。SG は 1 ペレットの値。
- 記録する値: そのニケの部隊画面の攻撃力（コンソール・装備込みの表示値そのまま）と、1 ヒットの 4 パターン。会心は 15% なので同条件の中で大きい方が会心。
- スキル 1・2 が通常攻撃の 1 発の値に影響しないキャラを使う（`packages/core/data` のスキル説明文から抽出。持っているものを優先）:

| 武器種 | 第一候補                            | 代替                                                         | 避ける例と理由                                                                   |
| ------ | ----------------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| AR     | クルミ (SR)                         | ラピ (SR)。ただし被弾 N 回で攻撃力▲が付くので序盤のみ        | ミハラ（最終弾で会心ダメ▲）、ソルジャーE.G.（命中時に確率で攻撃力▲）             |
| SMG    | I-DOLL・オーシャン (R、回復のみ)    | ミサト (SR、命中率▲は 1 発の値に無関係)                      | リリー（ランダム味方に攻撃力▲。単騎だと自分に乗る）、サクラ（N 回命中で被ダメ▲） |
| SG     | エーテル (SR)                       | ソルジャーF.A. (R)                                           | ネヴェ（フルバースト時に攻撃力▲。バーストを撃たなければ可）                      |
| SR     | デルタ (SR、HP・防御のみ)           | プロダクト08 (R、会心率▲は 1 発の値に無関係)                 | 姫野（SR 味方に攻撃力▲。単騎だと自分に乗る）                                     |
| RL     | I-DOLL・フラワー (R、敵攻撃力▼のみ) | アンカー (SR)                                                | ミカ（味方に装弾数▲。単騎だと自分に乗り、周期が変わる）、N102（チャージダメ▲）   |
| MG     | エマ (SSR、回復のみ)                | プロダクト12 (R)。ただし N 回攻撃で攻撃力▲が付くので序盤のみ | ソーダ・キロ（N 回攻撃で自己バフ）                                               |

読み取る 4 パターン（同じキャラ・同じ位置で）:

| 武器種 | キャラ | 実攻撃力 | 非コア・非会心 | コア・非会心 | 非コア・会心 | コア・会心 | 計算値（4 パターン） | 判定 |
| ------ | ------ | -------- | -------------- | ------------ | ------------ | ---------- | -------------------- | ---- |
| AR     |        |          |                |              |              |            |                      |      |
| SMG    |        |          |                |              |              |            |                      |      |
| SR     |        |          |                |              |              |            |                      |      |
| RL     |        |          |                |              |              |            |                      |      |
| SG     |        |          |                |              |              |            |                      |      |
| MG     |        |          |                |              |              |            |                      |      |

**結果（2026-09-22、録画の HUD 総ダメージカウンターを 1 ヒットごとに読み取り）**

| 武器 | キャラ           | 読み取った 1 ヒット                                                                                                      | 内訳の解釈（x = 素の 1 ヒット）                                                           |
| ---- | ---------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- |
| AR   | クルミ           | 14,036 / 19,435 / 24,834 / 30,232                                                                                        | 1.3x / 1.8x / 2.3x / 2.8x（距離 +0.3、会心 +0.5、コア +1.0 が 0.5x 刻みで加算）x = 10,798 |
| SMG  | ミサト           | 8,329 / 11,533                                                                                                           | 1.3x / 1.8x（距離、距離 + 会心）x = 6,407                                                 |
| SR   | デルタ           | 208,626 ×3、260,783                                                                                                      | 2.0x（コア）と 2.5x（コア + 会心）                                                        |
| RL   | I-DOLL・フラワー | 166,172 ×4                                                                                                               | 2.0x（コア、距離なし）                                                                    |
| RL   | ベロータ         | 407,357 ×5                                                                                                               | 2.0x（コア）、チャージ ×3.5                                                               |
| MG   | エマ             | 非会心非コア 6,116 / 会心非コア 9,174 / 非会心コア 12,233 / 会心コア 15,291（2026-09-23 再読み取り、4 パターンとも確認） | 1.0x/1.5x/2.0x/2.5x（**距離なし**、会心+0.5・コア+1.0が加算）                             |
| AR   | フォルクヴァン   | 14,834 / 20,540 / 26,246 / 31,951（2026-09-23、HUD ヒットログを 6fps 相当でコマ送り読み取り）                            | 1.3x/1.8x/2.3x/2.8x（距離＋会心・コアが加算、比が理論値と完全一致）                       |
| SG   | イサベル         | 25,409（1 ペレット）/ 38,114 / 50,818 / 63,523                                                                           | 1.0x/1.5x/2.0x/2.5x（距離なし）                                                           |
| SG   | プロダクト23     | 15,701（1 ペレット）/ 23,552 / 31,403 / （会心+コアは録画内で未確認）                                                    | 1.0x/1.5x/2.0x（距離なし、3 パターンで一意に解けた）                                      |

### 属性ボーナス・距離ボーナスの見落とし（訂正、2026-09-23）

上記の測定はすべて **風圧（Wind）属性の的（"BigArms" という名の中型ラプチャー、9 本の録画全てで同一個体と確認）** に対して行っていた（ユーザー確認）。加えて、録画は敵が初期位置にいる状態で撮っており、**AR と SMG のみ適正距離ボーナス（+0.3）が乗る**（RL・SR・MG・SG は乗らない）こともユーザーが確認した。当初の逆算はいずれも考慮していなかった。

属性については `packages/core/src/element.ts` の相性表（炎→風→鉄→電撃→水→炎、左が右に有利、倍率 ×1.1）に 6 体の属性を照らすと、**6 体中エマ（灼熱）のみ**が風属性の的に対して有利になる。残り 5 体（デルタ＝風圧、クルミ＝鉄甲、ベロータ＝電撃、ミサト＝鉄甲、フラワー＝風圧）はいずれも中立で影響なし。距離については AR（クルミ）・SMG（ミサト）の読み取りはもともと「距離あり」で解釈しており訂正不要。**訂正が必要なのはエマ（属性 ×1.1 の見落とし、かつ元々「距離あり」と誤って解釈していた MG の読み取り）だけ**だった。

2026-09-23、ユーザーがエマの録画を 4 パターンとも読み取り直し、正確な値（非会心非コア 6,116・会心非コア 9,174・非会心コア 12,233・会心コア 15,291）を提供。この 4 値の比は 1 : 1.4999 : 2.0002 : 2.5002 と、**距離ボーナスなし**（boost = 1.0/1.5/2.0/2.5）の場合の理論比 1:1.5:2.0:2.5 に極めて高い精度で一致し、以前の「距離あり（1.3x）」という解釈が誤りだったことを裏付ける。攻撃力 A（＝実攻撃力−100）と武器倍率 0.0557・属性倍率 ×1.1 を使い、4 値すべてを四捨五入で再現できる整数 A を 90,000〜110,000 の範囲で全探索すると **A=99,825 の一意解**が得られた（4 パターン全て誤差 0）:

```
攻撃力 = 100 + A = 99,925
```

この値は旧データ（84,573、距離ありと誤解釈・属性補正なし）とは全く別物で、以前この節で行った「エマのデータを無効化する」という判断は撤回する。

結論:

- 倍率グループの構造（1 + コア 1.0 + 会心 0.5 + 距離 0.3 の加算、チャージ倍率と武器倍率の乗算）は 6 武器種で一致した。
- 「戦闘中の攻撃力（逆算値）」と「Lv400 無凸の計算値」の関係を、当初は「一律 ×1.12365 の乗算係数」と解釈していたが、**2026-09-23 に誤りと判明**。正しくは後述のとおり「装備・好感度はいずれも割合ではなく固定値の加算」であり、キャラごとに基礎攻撃力が異なるため一律の乗算係数では説明できない（SR を 2 凸として再計算したことで偶然 ×1.12365 に近い値が出ていた）。詳細は次項。

### スペック固定の仕様（2026-09-23 再検証）

公式のお知らせ・パッチノート・ヘルプで「スペック固定」の数値仕様（Lv・凸・装備ティア等）を明記した一次資料は見つからなかった（下記「調査した情報源」）。そのため、ユーザーが過去に独自作成・検証してきた計算ツール（`D:\NIKKE_Damage_Calculator\シミュレーター本体.html`、以下「参照シミュレータ」）の `applyFixedSpecBuilds()` / `applyFixedSpecBuildForRid()` 実装と、本リポジトリの 1 ヒット実測（上表）を突き合わせて仕様を再構成した。参照シミュレータの成長曲線（レベル×グレード/コアの式）はユーザー本人が「計算式で無理やり近似させたもの」と明言しており誤差を含むため、**素のステータスは本リポジトリの `computeStat`（Stage 1 で ShiftyPad と一致確認済み）を正とし、参照シミュレータからは装備・好感度の離散テーブル値のみを採用した**。

**ビルド仕様**（参照シミュレータの実装 + 実測との一致で裏付け）:

- Lv400（`state.syncLevel = 400`）
- 限界突破・コア強化: レア度の**上限まで**（SSR は 3 凸+コア 7、SR は 2 凸、R は凸なし。2026-09-23 に SSR も含め確認済み）
- スキル 1・2・バースト: 全 Lv10
- 装備: 4 部位とも T9 Lv5、オーバーロード（OL）ステータスは「なし」×3 スロット
- キューブ: なし、コレクション（人形）強化: 0（未強化）、企業タワー / リサイクルルーム（コンソール）: 全項目 0
- 好感度: rank30（ピルグリム所属は rank40。2026-09-23 に非ピルグリム SSR 2 体・ピルグリム SSR 1 体で確認済み）
- 宝もの: キャラごとに個別設定（`FIXED_SPEC_TREASURE_STAGE`）。今回の参照 6 体はいずれも対象外（0）。参照シミュレータのリストは SSR 18 体のみが対象（うち 16 体は Stage3=解放済み、シュガーは Stage2、ロザンナは Stage1）。宝ものはスキル説明文の差し替えにのみ使われ、基礎ステータス（攻撃力・HP・防御）には影響しない実装だった。

装備（T9 Lv5）のステータス加算値は **クラス別の固定値の合算**（攻撃力に対する割合ではない）。参照シミュレータの内部テーブル（頭/胴/腕/足の 4 部位合計）:

| クラス | 攻撃力合計 | HP 合計 | 防御合計 |
| ------ | ---------- | ------- | -------- |
| 支援型 | 7,349      | 220,350 | 1,466    |
| 防御型 | 5,879      | 242,387 | 1,612    |
| 火力型 | 8,818      | 198,316 | 1,319    |

好感度の攻撃力加算値（クラス別ランクテーブル、抜粋）。参照シミュレータは「凸段階に応じて解放される好感度上限ランク」という仮説（rank10=0 凸、rank20=1 凸、rank30=2 凸以上、企業がピルグリムの個体のみ rank40）を採用している:

| クラス | rank10 | rank30 | rank40 |
| ------ | ------ | ------ | ------ |
| 支援型 | +336   | +1,367 | +1,950 |
| 防御型 | +269   | +1,094 | +1,560 |
| 火力型 | +403   | +1,640 | +2,340 |

**コア強化の適用対象（2026-09-23 解決）**: 当初は「本リポジトリの `computeStat`（grade/core 込み） + 好感度 + 装備」という単純な加算で突き合わせており、SSR（コア強化を持つ唯一のレア度）だけ +0.19% の誤差が残っていた。原因を調べたところ、[arca.live の解説記事](https://arca.live/b/nikketgv/95503431)（"명함과 돌파, 호감도/장비별 딜량 정리"）に次の記述を発見した:

> 돌파는 "기본 스탯의 2% 비율 + 고정수치 20 보정"의 비율로 상승 후 호감도/콘솔 스탯 합계<br>
> 코어강화는 "최종 합계된 풀돌 상태의 스탯"의 2% 비율로 상승
> （突破は「基礎ステータスの2%比率＋固定値20補正」で上昇後、好感度・コンソールのステータスを合算。コア強化は「最終的に合算された全突破状態のステータス」の2%比率で上昇）

つまり **コア強化の +2%/段は、突破適用後の素のステータスに「好感度」を加算した値に対してかかり、装備には掛からない**。本リポジトリの `computeStat` は突破後の素のステータスのみにコア%を掛けており（Stage 1 で ShiftyPad の「装備・好感度抜き」表示と一致確認済みなので、この式自体は正しい）、好感度分にはコア%がかかっていなかった。これが SR/R では誤差が出ず（コア強化を持たないため無関係）、SSR でだけ +0.19% の系統誤差になっていた原因である。

**戦闘中攻撃力の正しい計算式**:

```
gradeBase   = computeStat(char, 'attack', { level: 400, grade: <レア度上限>, core: 0 })  // 突破のみ、コアなし
withAffinity = gradeBase + 好感度加算(クラス, rank)
withCore     = withAffinity × (1 + 0.02 × core)                                          // コアはここに掛かる
combatAttack = round(withCore) + 装備加算(クラス, T9Lv5)                                  // 装備はコアの外
```

この式で、SSR 3 体すべてが**誤差ゼロで完全一致**した:

| キャラ         | レア | grade/core | 好感度 | gradeBase | +好感度 | ×(1+0.02×7) | +装備  | 計算値  | 実測    | 差                 |
| -------------- | ---- | ---------- | ------ | --------- | ------- | ----------- | ------ | ------- | ------- | ------------------ |
| エマ           | SSR  | 3/7        | rank30 | 79,840    | 81,207  | 92,575.98   | +7,349 | 99,925  | 99,925  | **±0（完全一致）** |
| フォルクヴァン | SSR  | 3/7        | rank30 | 63,884    | 64,978  | 90,969.08   | +5,879 | 79,954  | 79,954  | **±0（完全一致）** |
| イサベル       | SSR  | 3/7        | rank40 | 95,797    | 98,137  | 111,875.82  | +8,818 | 120,694 | 120,694 | **±0（完全一致）** |

SR/R は元々コア強化を持たない（コア = 0 固定）ため、この式は既存の「単純加算」モデルと数学的に同一になり、以前の突き合わせ結果（9 体中 7 体で誤差 0.01% 以内）はそのまま有効。**これで SR・R・SSR 全 9 体が、レア度に応じた単一の式で誤差ゼロ〜0.01% 以内に収まった**。

**好感度 rank30 と rank40 の切り分け（旧未確定 2）も解決**: 非ピルグリムの SSR 2 体（エマ・フォルクヴァン）は rank30 で完全一致。ピルグリムのイサベルは rank30 では大きくズレるが rank40 で完全一致し、**「ピルグリム所属のみ好感度上限が rank40」という参照シミュレータの仮説が正しい**と確定した。これは [game8.jp の解説記事](https://game8.jp/nikke/537260) や検索で見つかった複数の攻略記事とも整合する（「無凸は rank10、2 凸で rank30 に更新、ピルグリム所属のみ 3 凸以降 rank40」）。また [dcinside/arca.live の記事](https://arca.live/b/nikketgv/95503431) により「コア強化は好感度と無関係に 2% ずつ素のステータスを底上げするだけで、好感度ランクには影響しない」ことも確認済み（SSR は 3 凸で好感度上限に達し、コア強化を追加しても好感度ランクは変わらない）。

装備データ自体（T9Lv5 のクラス別固定値）は、この式変更によって R（プロダクト23、完全一致）・SR（4 体、誤差 0.01% 以内）・SSR（3 体、完全一致）のすべてで矛盾なく説明でき、**データ自体に誤りはなかった**と判断できる。

**未確定（要追加検証）**:

1. ~~SSR の限界突破・コア強化~~ → **解決**。レア度上限（3 凸+コア 7）。
2. ~~SSR の好感度上限~~ → **解決**。非ピルグリムは rank30、ピルグリムは rank40。
3. ~~非ピルグリム SSR の +0.19% の系統誤差~~ → **解決**。コア強化の +2%/段が「突破後の素のステータス＋好感度」に掛かり、装備には掛からないという計算順序の誤りだった（上記参照）。
4. R・SR の「好感度上限が凸段階に紐づく」という仮説自体の裏付けは、複数の攻略記事（game8.jp 等）で「無凸=rank10、2凸=rank30、ピルグリムは3凸以降=rank40」という記述が見つかり、裏付けが取れた。ただし「そのレア度の好感度絶対上限」との違いを完全に切り分けたわけではない（R/SR は無凸〜2凸の範囲でしか検証していないため）。
5. 参照シミュレータの装備 T9Lv5 テーブルの数値自体の一次資料（データマイニングか実測か）は依然として未確認だが、9 体全てで矛盾なく説明できたため実用上の信頼性は高い。
6. 公式一次資料で「スペック固定」の数値仕様を明記したものは見つからなかった。見つかった情報源は「射撃場でスペック固定が可能」という機能説明止まり（下記「調査した情報源」）。

式の検証としては、**SR・R・SSR 全レア度で Stage 2 の完了条件を満たした**。grade/core はレア度上限、好感度は非ピルグリム rank30・ピルグリム rank40、コア強化の +2%/段は「突破後の素のステータス＋好感度」に対して掛かり装備には掛からない、装備は T9Lv5 の固定値加算（コアの外）——このモデルで、9 体中 8 体が誤差 0.01% 以内、残り 1 体（クルミ）も −0.01% で実質誤差なしと言える。系統誤差は解消済みで、追加の未確定事項はない。

### 実装（2026-09-23）

上記の式を `packages/core/src/fixedSpec.ts` の `computeFixedSpecAttack` に実装した（装備・好感度のクラス別テーブルを同梱）。`fixedSpec.test.ts` で SSR 3 体（エマ・フォルクヴァン・イサベル）は誤差 0、SR/R 5 体は読み取り精度の範囲で一致することを固定値化している。calc には「ユニオン射撃場スペック固定」のチェックボックスを追加し、ON にすると Lv400・レア度上限の凸/コア・敵防御 100・戦闘時間 90 秒に切り替わり、戦闘中攻撃力（装備・好感度込み）で計算する。

### 調査した情報源

- [射撃場での【スペック固定】のみの仕様です #NIKKE](https://x.com/Reign_NIKKE/status/1915196989563953269) — X ポスト。本文は有料壁（HTTP 402）で確認できず、タイトルのみ検索結果経由で確認。
- [ユニオン - 勝利の女神：NIKKE wiki JP（有志メガニケ攻略wiki）](https://wiki3.jp/nikke/page/564) — ユニオン全般のページ。スペック固定の数値仕様の記載なし。
- [승리의 여신: 니케/콘텐츠 - 나무위키](https://namu.wiki/w/%EC%8A%B9%EB%A6%AC%EC%9D%98%20%EC%97%AC%EC%8B%A0:%20%EB%8B%88%EC%BC%80/%EC%BD%98%ED%85%90%EC%B8%A0) 8.2 節「사격장」— 「属性レベル固定の有無とタイマー設定機能がある」という機能説明のみ。数値仕様の記載なし。
- 神ゲー攻略・ゲームウィズ等の射撃場解説記事（検索結果より） — 「現在ピックアップ中のニケを編成して試せる」「スキルレベルや装備の強化状況を統一できる」という説明はあるが、具体的な数値（Lv/凸/装備ティア）の記載は見つからなかった。
- [명함과 돌파, 호감도/장비별 딜량 정리 - arca.live](https://arca.live/b/nikketgv/95503431) — **戦闘中攻撃力の式を確定させた決め手。** 「突破は基礎ステータスの2%+固定値20で上昇し好感度・コンソールを合算、コア強化は最終合算後の全突破状態のステータスの2%で上昇（装備は対象外）」という記述が、本リポジトリの実測（SSR 3 体で誤差ゼロ）と完全に一致した。
- [限界突破のメリットとコア強化の最大・おすすめのタイミング - game8.jp](https://game8.jp/nikke/537260) / 好感度ランク解説記事各種（検索結果より） — 「無凸は好感度上限ランク10、2凸でランク30に更新、ピルグリム所属のみ3凸以降ランク40」という記述があり、参照シミュレータの好感度モデルと一致した。
- 結論: 公式・wiki を含め、スペック固定の数値仕様（Lv・装備ティア等）を明記した一次資料は見つからなかった。一方、コア強化・好感度ランクの計算式については複数の攻略記事・コミュニティ解説で裏付けが取れ、本リポジトリの実測（9 体、誤差ゼロ〜0.01%）と完全に整合した。

読み取りのコツ:

- 会心は 15% なので、同じ条件で数発撃てば「大きい方の値」が会心。コアは狙う位置で切り替える。
- SG は 1 ペレットごとに数値が出る。ペレット単価で比較する。
- 距離ボーナスの有無は、適正距離の内外で撃って 1.3 倍差が出るかで確認できる。

## Stage 2-B: 発射サイクル（時間の実測）

**結果（2026-09-22）: 較正完了。** `E:/nikke_project_captures/range/` の 4 本（`20260922-01`〜`-04`。60fps、Xbox Game Bar、AV1。対応は [captures/index.md](captures/index.md)）を ffmpeg で生フレーム化し、手元領域の動き量（反動）・的の着弾・HUD の総ダメージカウンターの変化をフレーム単位で追った。`20260922-01_SR_delta.mp4` は冒頭 5 秒にフレーム落ちがあるが、それ以降は 60fps で安定（平均 48.8fps）。フレーム番号の数え方については [captures/index.md](captures/index.md) の「フレーム番号の約束」を参照。

| 武器                  | キャラ           | 実測                                                                                              | モデル（較正後）                                                                | 差           |
| --------------------- | ---------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------ |
| SR                    | デルタ           | フルチャージ連射の間隔 82f（551→633→715）。1 発目→次マガジン 1 発目 577f                          | 82f = チャージ 60f + 解放遅延 22f。サイクル 82 + 5×82 + リロード 90 = 582f      | −5f（−1%）   |
| RL                    | I-DOLL・フラワー | 間隔 82f（832→914→996→1078→1160→1242）。6 発目→次の 1 発目 200f。サイクル 610f                    | 82f。6 発目→次 1 発目 = リロード 120 + 82 = 202f。サイクル 612f                 | −2f          |
| AR                    | クルミ           | 間隔 5f（62 パルス中 54 が 5f）。60 発で 295f。最終弾→次マガジン 1 発目 59f                       | 5f、295f、リロード 60f で即射撃。サイクル 355f                                  | ±1f          |
| MG                    | エマ             | 弾切れ→装填完了 151f。装填完了→1 発目 約 20f。1 発目→300 発目 387f。1 発目→次マガジン 1 発目 563f | リロード 150f、初弾遅延 20f、レート蓄積で 390f。サイクル 560f                   | +3f（+0.5%） |
| SMG                   | ミサト           | 総ダメージカウンターの変化が周期 5f（2 発）で自己相関し、120 発が約 298f                          | レート蓄積で 3,2,3,2… の 2.5f 平均（24 発/秒）。切り上げ 3f（20 発/秒）ではない | ±2%          |
| RL（チャージ 1.5 秒） | ベロータ         | 間隔 112f（1012→1124→…→1460）。6 発目→次 1 発目 230f。サイクル 790f                               | 90f + 22f = 112f。リロード 120 + 112 = 232f。サイクル 792f                      | −2f          |

モデルの変更点（`packages/core/src/cadence.ts`, `weapons.ts`）:

- チャージ武器: 毎発 `チャージ時間 + 22f`。リロード後の 1 発目も同じ（RL の 200f = 120 + 82 で確認）。`chargeReleaseFrames = 22`（参考 OSS の値と一致）。チャージ 1.5 秒（ベロータ）でも 90 + 22 = 112f で一致。
- 非チャージ武器: 発射レートを 1 フレームごとに蓄積し、1 発分たまったフレームで発射（フレーム切り上げではない）。AR 720rpm は 5f 固定。MG は「60rpm 開始、1 発ごとに +100rpm、3600rpm 上限」で 1 発目→300 発目が 390f（実測 387f）。以前の切り上げモデル（467f）は誤りだった。
- リロード後の 1 発目: AR は即射撃（遅延 0）。MG は約 20f の初弾遅延（`spinUpFirstShotFrames = 20`）。
- サイクル = 初弾遅延 + マガジン（1 発目→最終弾） + リロード。以前の「装弾数 × 間隔 + リロード」から変更。
- SMG（1440rpm）は蓄積モデルの 2.5f 平均（24 発/秒）で一致（ミサト）。参考 OSS の切り上げ 3f 説は誤り。
- SG（90rpm = 40f）は未計測。蓄積でも切り上げでも 40f で同じ。

計測手順のメモ: 発射の検出には、SR/RL は手元の反動（フレーム差分の局所平均）、AR は同じ手元差分のパルス列、MG は HUD の総ダメージカウンター領域の変化（1 ヒットごとに更新）が有効だった。残弾カウンターは数字がロール表示されるため毎フレーム変化してしまい使えない。

### 録画条件（2-B）

- 録画: PC 版を 60fps で録画（Xbox Game Bar の Win+G、または OBS。設定で 60fps を確認）。解像度は問わない。戦闘開始の合図から最初のリロード完了後の 1 発目までを含める（15 秒程度）。
- 操作: 射撃ボタンを押しっぱなしにするだけ。カバー・バーストはしない。SR/RL はフルチャージ攻撃を連続で撃つ（押しっぱなしで自動的にフルチャージ連射になる）。
- 未計測で残っているもの: SG（90rpm = 40f、分割リロードの実時間）。動画を `E:/nikke_project_captures/range/` に置き、[captures/index.md](captures/index.md) の命名規約で名前を付けて台帳に 1 行足せば、同じ手順（`tools/captures/`）で計測できる。

## Stage 2-C: 目安（総ダメージ）

較正には使わない。90 秒または 180 秒の射撃場の総ダメージが、calc の結果（戦闘時間を合わせる）に対して ±10% 程度に収まるかだけ見る。ズレの原因はマガジン周期の端数、距離・ジャンプによる命中差、スキルの混入など多数あるため、外れても原因の特定はしない。

| キャラ | 時間 | 実測 | 計算 | 差  | メモ |
| ------ | ---- | ---- | ---- | --- | ---- |
|        |      |      |      |     |      |

## Stage 3: 5 人編成の合算

判定基準: 編成の合計が、各枠を単体で計算した結果の和と一致すること（[design-stage3.md](design-stage3.md) 4 節）。射撃場の実測は較正に使わない（5 体で入ると味方のスキル・バフが通常攻撃に乗り、「通常攻撃のみ」のモデルと比較できない）。

**結果（2026-09-22）: 完了条件を満たした。** 自動テストは core 7 件・calc 10 件、手動確認はクルミ (AR)・ミサト (SMG)・デルタ (SR)・I-DOLL・フラワー (RL)・エマ (MG) の 5 体で行った。

| 確認項目                                 | 方法                | 結果                                                                                                                                                                               | 日付       |
| ---------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 合計 = 各枠を個別に計算した和            | core `team.test.ts` | 一致（DPS・総ダメージとも）。1 枠のみの編成は computeDamage と同一                                                                                                                 | 2026-09-22 |
| 空枠の無視・重複・枠数超過で例外         | core `team.test.ts` | 空枠は合計から除外、重複・0 枠・6 枠で RangeError                                                                                                                                  | 2026-09-22 |
| reducer と永続化の復元                   | calc `team.test.ts` | 重複選択は無視、clearSlot は育成値を保持、不正 JSON・未知 id・重複 id・範囲外は null                                                                                               | 2026-09-22 |
| 5 体入力で内訳表の合計が各行の和と一致   | ブラウザで手動      | DPS 248,908 = 各行の和。総ダメージ 44,803,366 に対し表示値の和は 44,803,365（表示桁の丸め）                                                                                        | 2026-09-22 |
| 1 枠を外すと合計がその行の分だけ減る     | ブラウザで手動      | スペック固定時 112,069,050 → 102,941,806（フラワーの行 9,127,244 と一致）                                                                                                          | 2026-09-22 |
| スペック固定 ON が全枠に効く（防御 100） | ブラウザで手動      | 全枠 Lv400・レア度上限に固定、攻撃力はエマ 99,925・デルタ 63,368・フラワー 54,316（Stage 2-A の逆算値と一致、±1）。戦闘時間は 180 のまま（ユーザー指示で 90 秒への切り替えを廃止） | 2026-09-22 |
| 他枠で選択中のニケが選択肢に出ない       | ブラウザで手動      | 枠 1 の選択肢からミサト・デルタ・フラワー・エマが消える                                                                                                                            | 2026-09-22 |
| リロード後に編成が復元される             | ブラウザで手動      | 4 体 + スペック固定 ON がそのまま復元                                                                                                                                              | 2026-09-22 |
| 幅 375px で横スクロールが出ない          | ブラウザで手動      | scrollWidth 375 = innerWidth。内訳表はコンテナ内スクロール                                                                                                                         | 2026-09-22 |

## Stage 4: スキルモデル段階 A — 常時発動パッシブ

判定基準: [design-stage4.md](design-stage4.md) 5 節。自動テストで式と合算の正しさを固定し、射撃場のスペック固定・1 ヒット実測（Stage 2-A と同じ方法）でバフの効き方を確かめる。

**結果（2026-09-22）: 実装と自動テスト・ブラウザ確認は完了。射撃場の実測は同日に 4 編成とも実施し、`attack`（自己）・`attack`（発動者基準）・`chargeDamage`（自己・味方）は 1 ヒットの 4 パターンが 1 の位まで一致。`attackDamage` は値（+0.30）は合うが、式の中の置き場所が違った（会心・コアの加算グループの外で乗算。下記）。同日、core の式を別枠の乗算に訂正し（`attackDamageMultiplier`）、訂正後の予測で 4 パターンが一致。5.3 の完了条件を満たした。** 自動テストは core 12 ファイル 102 件・calc 15 件（合計 117 件）。定義済みキャラはマナ (290)・ノワール (271)・ウンファ：タクティカル・アップ (95)・エマ：タクティカル・アップ (93)・クイーン（真）(870) の 5 体。

### 自動テスト・ブラウザ確認

| 確認項目                                                   | 方法                               | 結果                                                                                                                                                                                | 日付       |
| ---------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| `skillValue` が Lv1 / Lv10 で説明文の両端の値を返す        | core `skills/resolve.test.ts`      | 生の数値を返し、範囲外の ref / Lv・空スロット・非数値は RangeError                                                                                                                  | 2026-09-22 |
| `resolvePassives` が 100 で割った比率にし scaling を埋める | core `skills/resolve.test.ts`      | Lv1 → 0.10、Lv10 → 0.19。unsupported は空。別キャラの定義は RangeError                                                                                                              | 2026-09-22 |
| `parseSkillDefinition` の検証                              | core `skills/resolve.test.ts`      | casterAttack + 非 attack、assumes / notes が LocalizedText でない、formatVersion 違い、ref 0、未知のスロット、burst の対応、support と effects の不整合をすべて Error               | 2026-09-22 |
| バフ適用の純関数                                           | core `skills/buffs.test.ts`        | `base × (1 + ratio) + flat`、会心の加算、チャージは `charge` のときだけ加算                                                                                                         | 2026-09-22 |
| `computeDamage` の `buffs`                                 | core `damage.test.ts`              | 未指定は ZERO_BUFFS と同一。攻撃力バフは防御減算の前、会心・攻撃ダメージは倍率グループ、チャージは AR に効かず RL のフルチャージ時だけ、`attackOverride` の上にも乗る               |
| 編成での配り方                                             | core `team.test.ts`                | 定義なし編成は Stage 3 と同一。self は発動元だけ、allies は skills 省略の枠を含む全枠。casterAttack は発動者のバフ前攻撃力（attackOverride 優先）。枠を外すとその枠の効果だけ消える |
| 定義ファイルとキャラデータの整合                           | core `skills/definitions.test.ts`  | 5 体すべて: index と過不足なし、ref が 10 要素の数値、Lv で単調非減少、burst は unsupported、非 supported には notes あり                                                           | 2026-09-22 |
| スキル Lv の状態と永続化                                   | calc `team.test.ts`                | 既定 10、枠ごとに更新、clearSlot で保持、スペック固定で 10 固定、Stage 3 の保存データ（skillLevels なし）は 10 に補完、0 / 11 / 小数 / 文字列は null                                | 2026-09-22 |
| 5 体（スペック固定）の攻撃力が手計算と一致                 | ブラウザ + node で core を直接呼ぶ | マナ: 119,896 × 1.5808 + 119,896 × 0.1408 = 206,412.95 で画面の 206,413 と一致。合計 683,647,347 = 各行の和                                                                         | 2026-09-22 |
| スキル Lv を変えると数値が追従                             | ブラウザで手動                     | マナ スキル 1 を Lv10 → Lv1 で「攻撃力 +58.08%」→「+34.32%」、説明文も 34.32％ に変わり、攻撃力 22,416 × 1.3432 + 3,156 = 33,265                                                    | 2026-09-22 |
| 未定義キャラの表示                                         | ブラウザで手動                     | クルミを枠 5 に入れると「未定義 スキル定義なし（通常攻撃のみで計算。味方からのバフは受ける）」。ノワール等の味方効果は受けている                                                    | 2026-09-22 |
| スペック固定で Lv 入力が disabled、OFF で有効              | ブラウザで手動                     | ON で disabled・Lv10、OFF で入力可                                                                                                                                                  | 2026-09-22 |
| リロード後にスキル Lv と編成が復元                         | ブラウザで手動                     | Lv1 のマナと枠 5 のクルミがそのまま復元                                                                                                                                             | 2026-09-22 |
| 幅 375px で横スクロールが出ない                            | ブラウザで手動                     | scrollWidth 375 = innerWidth                                                                                                                                                        | 2026-09-22 |

### 射撃場の実測（スペック固定・1 ヒット）

**実施（2026-09-22、録画 10〜13。[captures/index.md](captures/index.md)）。** 予測は calc の「攻撃力（バフ後）」から `(攻撃力 − 100) × 武器倍率 × チャージ倍率 × (1 + コア 1.0 + 会心 0.5 + 距離 0.3) × 属性` で出した（Stage 2-A の式）。読み取りは HUD 総ダメージカウンターのフレーム間差分を主とし、的の上に出るダメージ数値で裏を取った。

測定条件（Stage 2-A と違う点だけ）:

- ユニオン射撃場の **灼熱タブ**。的は同じ `BigArms` だが **属性は灼熱**（WEAK POINT 表示は水冷）。スペック固定 ON（出撃画面に「スペック固定有効」の表示、録画 10 のフレーム 60）。防御 100 相当は下記の一致で裏付け。
- 攻撃側の属性はマナ・ノワール・デルタが風圧、アスカ・クイーン（真）・ウンファが灼熱で、**いずれも灼熱の的に対して有利にならない**（`element.ts` の相性表で灼熱に有利なのは水冷だけ）。属性倍率はすべて ×1.0。
- 距離ボーナスは AR（マナ・アスカ）のみ +0.3（初期位置。Stage 2-A の知見どおり）。SG・SR は乗らない。
- 2 体編成のときは、定義のない味方（アスカ rid 830、デルタ rid 20）を受け手にして「味方への効果」を切り出した。アスカのスキルは回復時・フルバースト時発動で通常時は無関係、デルタは HP・防御のみ。
- スペック固定の攻撃力は `computeFixedSpecAttack` の値（SSR 火力型 5 体はいずれも 119,896、SR 防御型のデルタは 63,368）。

| 確認したいこと                   | 編成（録画）                 | 予測（calc の値。非コア非会心 / 会心 / コア / コア会心）                                                                                                                                                                                                       | 実測（1 ヒット）                                                                                                                                      | 結果                                                                                                                |
| -------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `attack`（自己）                 | マナ 1 体（10）              | 攻撃力 119,896 × 1.5808 = 189,531.6。1 ヒット = 189,431.6 × 0.1507 × {1.3, 1.8, 2.3, 2.8} = 37,111.5 / 51,385.2 / 65,658.9 / 79,932.6                                                                                                                          | 37,112 / 51,385 / 65,659 / 79,933                                                                                                                     | **一致（4 パターンとも誤差 0）**                                                                                    |
| `attack`（発動者基準・固定加算） | ノワール + アスカ（11）      | ノワールの攻撃力 119,896 × 0.1408 = +16,881.4 を 2 人に加算 → 136,777.4。アスカ 1 ヒット = 136,677.4 × 0.1876 × {1.3, 1.8, 2.3} = 33,332.9 / 46,153.2 / 58,973.5。ノワール 1 ペレット = 136,677.4 × 0.2046 × {1, 1.5} = 27,964.2 / 41,946.3                    | アスカ 33,333 / 58,973（会心は未観測）。ノワール 1 発 279,640 = 27,964 × 10（2 回）、209,730 = 27,964 × 6 + 41,946、307,604 = 27,964 × 8 + 41,946 × 2 | **一致（アスカ誤差 0、ノワールはペレット合計が非会心・会心の整数結合で説明できる）**                                |
| `attackDamage`                   | クイーン（真）1 体（12、14） | 訂正後の式: 119,796 × 0.2015 × (1 + 0.30) × {1, 1.5, 2, 2.5} = 31,380.6 / 47,070.8 / 62,761.1 / 78,451.4。戦闘開始 15 秒以内は S1 の攻撃力 +50.28% が乗り 36,286.06 × 1.3 × {…} = 47,171.9 / 70,757.8 / 94,343.8 / 117,929.7                                   | 15 秒以内（12）: 47,172 / 70,758 / 94,344 / 117,930。15 秒経過後（14）: 31,381 / 47,071（コアは未観測）                                               | **一致（訂正後の式で 15 秒以内の 4 パターン、15 秒経過後の 2 パターンとも誤差 0）**                                 |
| `chargeDamage`                   | ウンファ：TU + デルタ（13）  | ウンファ: 攻撃力 119,896 × 1.4224 = 170,540.1、チャージ 2.5 + 0.4181 = 2.9181 → 170,440.1 × 0.6904 × 2.9181 × {1, 1.5, 2, 2.5} = 343,378 / 515,067 / 686,756 / 858,445。デルタ: 63,268 × 0.6595 × 2.9181 × {…} = 121,758.4 / 182,637.7 / 243,516.9 / 304,396.1 | ウンファ: コア 686,756、コア会心 858,445（非コアは未観測）。デルタ: 121,758 / 182,638 / 243,517 / 304,396                                             | **一致（デルタ 4 パターン誤差 0。ウンファの自己 `attack` +42.24% と味方への `chargeDamage` +0.4181 を同時に確認）** |

補足:

- `critRate`（ウンファの味方全体 +8.16%）は確率なので 1 ヒットでは切り出せない。式は参考資料どおりのまま、未実測。
- `critDamage`（エマ：TU）は今回撮っていない。未実測。
- 録画 13 の最初の数発は 2 人が同じフレームで着弾し、HUD が 930,273（= 686,756 + 243,517）や 991,152（= 686,756 + 304,396）とまとめて増える。単独着弾（フレーム 1490→1491 で +686,756、1900→1901 で +858,445 など）で切り分けた。

#### attackDamage の置き場所（2026-09-22 訂正済み）

クイーン（真）の 4 パターンは 47,172 / 70,758 / 94,344 / 117,930 で、比が **1 : 1.5 : 2 : 2.5**。現行の式（`boostTotal = 1 + コア + 会心 + 距離 + attackDamage`）なら比は 1.3 : 1.8 : 2.3 : 2.8 になるはずなので、**攻撃ダメージ▲は会心・コアの加算グループの外で別に乗算されている**。

さらに絶対値は calc の予測より大きい。クイーン（真）のスキル 1 に「戦闘開始時、自分に攻撃力 50.28%▲（Lv10、15 秒間）」があり（Stage 6 扱いで未対応）、録画の戦闘は 10 秒で終わっているので全ヒットにこれが乗っている。両方を入れると:

```
(119,896 × (1 + 0.5028) − 100) × 0.2015 = 36,286.06
× (1 + 0.30) × {1, 1.5, 2, 2.5} = 47,171.9 / 70,757.8 / 94,343.8 / 117,929.7
```

4 値とも 1 の位まで一致する。加算グループに入れた場合の会心は 36,286 × 1.8 = 65,315 で実測 70,758 と合わない。

結論と対応:

- `attackDamage` の値（+0.30、Lv10）は正しい。式を `… × (1 + コア + 会心 + 距離) × (1 + Σ attackDamage)` に訂正した（`packages/core/src/damage.ts` の `attackDamageMultiplier`、`skills/buffs.ts` の `applyAttackDamageBuffs`、[design-stage4.md](design-stage4.md) 3.2 の表、calc の内訳表示は「倍率グループ合計」の次に「攻撃ダメージ（バフ）×1.3000」）。core の `damage.test.ts` で 1.3 倍が別枠で掛かることを固定した。
- 録画 12 は戦闘 10 秒で止めており、未対応の戦闘開始時バフ（15 秒）が全ヒットに乗っていた。**同日に追加撮影した録画 14（戦闘 24 秒）で分離した**: 15 秒経過を境にダメージ数値が 47,172 / 70,758 から **31,381 / 47,071** に切り替わり（フレーム 1700 の 1 発は 6 × 47,172 + 70,758、フレーム 1740 の 1 発から 9 × 31,381）、これはバフなしの訂正後の式 119,796 × 0.2015 × 1.3 = 31,380.6、× 1.5 = 47,070.8 と誤差 0 で一致する。加算のままなら会心は 24,139 × 1.8 = 43,450 になるはずで、これで attackDamage +0.30 と別枠乗算の両方が戦闘開始時バフと無関係に確認できた。
- 15 秒経過後の HUD 差分は 9 ペレットで 282,428 と 282,429 の両方が出る（10 ペレットは 313,809）。1 ペレットの真値が 31,380.9 前後の小数で、HUD は累計を丸めて表示していると解釈できる。数値ポップアップは 31381 で一定。
- 距離ボーナスや Stage 2-A の 6 武器種の結果は attackDamage が 0 なので影響しない。

## Stage 5: フルバースト基礎とシンプルバースト（sim 先行 → calc）

判定基準: [design-stage5.md](design-stage5.md) 6 節。sim（フレーム逐次）を先に作って cadence と 1 フレームもずれないことを固定し、calc（2 区間の期待値）との整合を自動テストで 3 段（厳密一致する量・離散化誤差の上限・長時間での収束）で固定する。射撃場の実測は「フルバースト中の通常攻撃 1 ヒット（+0.5）」と「バーストスキルの倍率ダメージ」。

**結果（2026-09-22）: 実装・自動テスト・ブラウザ確認に加えて、射撃場の実測（録画 15）でフルバースト補正が加算 +0.5 であることまで確認済み。バーストスキルダメージ側の +0.5 の有無は未実測。** 自動テストは core 16 ファイル 153 件・calc 17 件（合計 170 件）。定義済みキャラは既存 5 体（バーストを埋めた: ノワール `skill`、クイーン（真）`distributed`、マナ・エマ：TU・ウンファ：TU は `unsupported` + notes）+ ラピ (10) の 6 体。

### 自動テスト・ブラウザ確認

| 確認項目                                 | 方法                                                                   | 結果                                                                                                                                                                                                                                         | 日付       |
| ---------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 射手の状態機械が cadence と一致          | core `sim/__tests__/shooter.test.ts`                                   | AR / SMG / SR / RL / MG / SG / 分割リロード SG の 7 種で 3 マガジン分の発射フレーム列が `k × cycleFrames + first + shotFrames[i]` と完全一致。設計書 4.2 節の絶対フレーム（AR 0…295→355、SR 82…492→664、MG 20…410→580）も一致                | 2026-09-22 |
| 固定サイクルの時刻表                     | core `burst/__tests__/fixedCycle.test.ts`                              | 180 秒で発動 600, 1800, …, 10200 の 9 回・フルバースト 5,400f。端数（10,500f）は最後の区間が切れる。`isFullBurstFrame` の境界 599/600/1199/1200。段階の割当（枠順・AllStep の充当・空枠）                                                    | 2026-09-22 |
| バーストスキルダメージの式               | core `skills/__tests__/burstDamage.test.ts`                            | 武器倍率・コア・距離を掛けず、会心期待値・攻撃ダメージ・属性を掛ける。`BURST_SKILL_FULL_BURST_BONUS` の真偽で `boost.fullBurst` が 0 / 0.5。複数効果は合計。定義なし・unsupported は null                                                    | 2026-09-22 |
| 1 トリガーの式の切り出し                 | core `damage.test.ts`                                                  | `computeTriggerDamage` = `computeDamage` から cadence を除いた部分。`fullBurst: true` で `boost.total` が +0.5、省略は Stage 4 と同一                                                                                                        | 2026-09-22 |
| sim のフレームループ                     | core `sim/__tests__/engine.test.ts`                                    | バーストなしは全トリガーが非区間で周期から数えた数と一致（AR 180 秒 = 1,830 発）。バーストありは発射パターン不変で区間だけ変わる。割当枠が 9 回発動し `9 × perActivation`、同段階の 2 体目は 0 回。trace のイベント順（I → II → III → 射撃） | 2026-09-22 |
| calc の 2 区間モデル                     | core `team.test.ts`                                                    | `burst` なしは Stage 4 と同一（`schedule: null`）。`share` は新しい `totalDamage` 基準                                                                                                                                                       | 2026-09-22 |
| **sim と calc の整合（厳密）**           | core `simCalc.test.ts`                                                 | 時刻表・バフ・1 トリガー値（両区間）・バーストヒット・発動回数・バーストスキル合計が完全一致                                                                                                                                                 | 2026-09-22 |
| **sim と calc の整合（離散化誤差）**     | core `simCalc.test.ts`                                                 | 5 武器種の編成（180 秒）でトリガー数の差は 1 マガジン未満、枠の総ダメージの差は 5% 以内、編成合計 3% 以内。実測値は下表                                                                                                                      | 2026-09-22 |
| **sim と calc の整合（収束）**           | core `simCalc.test.ts`                                                 | 180 → 1,800 → 18,000 秒で相対差が縮み、18,000 秒で 0.5% 以内                                                                                                                                                                                 | 2026-09-22 |
| 定義ファイルの整合                       | core `skills/__tests__/definitions.test.ts`                            | 6 体: index と過不足なし、`burstDamage` の `ref` が 10 要素の数値（≥ 100%）、Lv で単調非減少、非 supported には notes                                                                                                                        | 2026-09-22 |
| バーストのトグルと永続化                 | calc `team.test.ts`                                                    | 既定 ON、`setBurst`、Stage 4 の保存データ（`burst` なし）は ON に補完、真偽値以外は null                                                                                                                                                     | 2026-09-22 |
| 表の各行の和が合計行と一致（`burst` ON） | ブラウザで手動（スペック固定、ノワール・クイーン（真）・ラピ・デルタ） | 総ダメージ 201,751,094 + 222,805,902 + 73,131,786 + 38,622,126 = 536,310,908 = 合計行。DPS の和 2,979,505 = 合計行。CLI（`npm run sim`）の calc 列とも一致                                                                                   | 2026-09-22 |
| `burst` OFF で Stage 4 と同じ表示        | ブラウザで手動                                                         | FB 列・バーストスキル表・時刻表の注記が消え、合計 481,026,605（ノワール 178,329,630 = 664,149 × 1.492 × 180）。ON に戻すと 536,310,908 に復帰                                                                                                | 2026-09-22 |
| 詳細パネルの 2 区間表示                  | ブラウザで手動                                                         | ノワール: 倍率グループ ×2.375 / ×2.875、1 トリガー 664,149 / 803,970、区間 90 秒 / 90 秒、バーストスキル 1 発動 516,658 = 136,677.4 × 3.5164 × 1.075（会心期待値）、9 回 = 4,649,924                                                         | 2026-09-22 |
| 段階が埋まらないときの注記               | ブラウザで手動                                                         | 「バースト I のニケがいません（フルバーストは起きると仮定）」と、発動枠（II: デルタ、III: ノワール）が出る                                                                                                                                   | 2026-09-22 |

### sim と calc の差（180 秒・5 武器種のフィクスチャ、`burst` ON）

`simCalc.test.ts` と同じ編成（AR 720rpm・60 発 / SMG 1440rpm・120 発 / SR チャージ 1 秒・6 発 / RL チャージ 1.5 秒・6 発 / MG スピンアップ・300 発。コア命中率 0.6・距離あり）。calc は「秒間トリガー数 × 区間の秒数」の期待値なので、差はマガジンの位相と端数だけ。

| 武器 | 周期 | トリガー数 sim / calc（180 秒） | sim のフルバースト区間の割合 | 枠の総ダメージの差 sim − calc |
| ---- | ---- | ------------------------------- | ---------------------------- | ----------------------------- |
| AR   | 355f | 1,830 / 1,825.35                | 49.67%                       | +0.181%                       |
| SMG  | 358f | 3,624 / 3,620.11                | 50.00%                       | +0.107%                       |
| SR   | 582f | 111 / 111.34                    | 50.45%                       | −0.205%                       |
| RL   | 792f | 82 / 81.82                      | 47.56%                       | −0.327%                       |
| MG   | 560f | 5,749 / 5,785.71                | 50.30%                       | −0.567%                       |

編成合計の差は 180 秒で −0.192%、1,800 秒で +0.037%、18,000 秒で +0.011%。テストの許容値（枠 5%・合計 3%・18,000 秒で 0.5%）には十分な余裕がある。

**位相ロックの知見**: MG だけは 18,000 秒でもフルバースト区間の割合が 50.13% に張り付く。MG の周期 560f とバーストサイクル 1,200f の最大公約数 80 が区間長 600 を割り切らない（600 / 80 = 7.5）ため、発射フレームの剰余が 80 刻みの格子に乗り、区間に入る割合が厳密には 1/2 にならない。AR（gcd 5）・SMG（2）・SR（6）・RL（24）は 600 を割り切るので長時間で 50% に収束する。固定サイクルが前提の Stage 5 では 0.04% 程度の系統差で実用上は無視できるが、「calc は sim の長時間平均」という言明には「周期がサイクルと通約する武器を除く」の但し書きが要る。Stage 7 で動的サイクルになれば消える。

### 射撃場の実測（スペック固定・1 ヒット）

Stage 2-A / 4 と同じ方法（HUD 総ダメージの差分と的の上の数値）。**自動バースト ON** で撮り、HUD の「FULL BURST」表示と時刻も読む。予測は calc（スペック固定、灼熱の的なら属性倍率 1.0）。

| 確認したいこと                                            | 編成                                                              | 予測                                                                                                                                              | 実測                                                                             | 結果                                                    |
| --------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------- |
| 通常攻撃のフルバースト +0.5                               | クルミ（AR）+ I: エーテル + II: デルタ + III: ノワール（録画 15） | 加算なら 1.3x → 1.8x、2.3x → 2.8x。別枠乗算なら ×1.5                                                                                              | 17,032 / 23,583 / 30,134 → 23,583 / 30,134 / 36,684                              | **一致（加算 +0.5。下の「録画 15」節）**                |
| バーストスキルダメージ（`skill`）とフルバースト補正の有無 | エーテル + デルタ + ノワール III（録画 19）                       | 乗らないなら 480,611、乗るなら 720,917                                                                                                            | **480,611（読み取った 4 回すべて）**                                             | **一致（乗らない。コア・距離も乗らない）**              |
| 分配ダメージ（`distributed`、単体なら全額）               | クイーン（真）+ I/II（火力に触らない 2 体）                       | 119,796 × 14.2169 × 1.30 × {1, 1.5} = 2,214,066 / 3,321,099（+0.5 が乗るなら 3,321,099 / 4,428,132）。S1 の戦闘開始時攻撃力▲（15 秒）が切れてから | 未実測                                                                           | 未実施                                                  |
| 固定サイクルの妥当性（参考）                              | 録画 18〜21 の時刻                                                | 1 回目のフルバースト開始が 10 秒前後か、10 秒続くか。III 1 体なら CT 40 秒で回るはず                                                              | **FB は 180 秒で 5 回・約 40 秒周期**（録画 18〜21。2026-09-23 に 4 回から訂正） | **CT 40 秒。固定 20 秒サイクルとの差は Stage 7 で扱う** |

**`BURST_SKILL_FULL_BURST_BONUS = false` は 2026-09-22 に確定した**（録画 18・19。詳細は Stage 6 節の実測）。ノワールのバースト 351.64% が読み取った 4 回とも 480,611 =（バフ後の整数攻撃力 136,777 − 100）× 3.5164 で、フルバースト補正もコアも距離も乗っていない。ラピのバースト 657.72% も 208,131 × 3 = 624,393 で同じ結論になる。

録画 15 でクルミの S2（追加ダメージ）は **コア倍率が乗る** ので、「スキルダメージ系は加算グループのうちコアだけが乗りうる」という仮説は残る。ただし `burstDamage`（バースト枠の倍率ダメージ）については **コアも乗らない** ことがノワール・ラピで確認できた。両者の違い（追撃系は乗る / バースト枠は乗らない）は段階 C で追加ダメージを扱うときに改めて確認する。

**固定サイクルとの差（Stage 7 の材料）**: 録画 18〜21 とも、フルバーストは 180 秒で **5 回**・**約 40 秒周期**（録画 19 の動画時刻で約 21 秒・60 秒・100 秒・139 秒・179 秒。戦闘開始は動画の約 14 秒）だった。当初は 4 回と数えていたが、2026-09-23 に BURST バーの画素読み取りで 5 回目（残り 00:12 付近、`frames/20260922-19_SG+SR+SG_ether+delta+noir_f10800_fb5.jpg`）を見つけて訂正した（[design-stage7.md](design-stage7.md) 0 節）。III が 1 体の編成ではバースト CT 40 秒がそのままサイクル長になる。固定 20 秒サイクルのモデルは 9 回と数えるので、**バーストスキルダメージの回数が実機の 1.8 倍、フルバースト稼働率が 50% 対 約 27.8%** になる。Stage 5 の 9 節に書いた限界が実測で裏付けられた形で、Stage 7 で CT・ゲージを入れて解消する。

### 録画 15: フルバースト補正は加算だった（2026-09-22 実施）

**実施（2026-09-22、録画 15。[captures/index.md](captures/index.md)）。** 目的はフルバースト突入時の「フルバースト補正（+50%）」が、通常攻撃のダメージ式において「加算グループ（+0.5）」なのか「別枠乗算（×1.5）」なのかを実測で白黒付けること。

#### 測定条件

- ユニオン射撃場の **灼熱タブ**。的は `BigArms`（灼熱属性、WEAK POINT 水冷、防御 100 相当）。スペック固定 ON（出撃画面で確認）。
- 編成: ノワール (SSR, 火力, 枠1) + デルタ (SR, 防御, 枠2) + クルミ (SR, 支援, 枠3) + エーテル (SR, 防御, 枠4)。
  - 操作キャラはクルミ（AR、適正距離）。
  - バースト発動順は エーテル (I: バリアのみ) → デルタ (II: デコイのみ) → ノワール (III: 全体ダメージのみ、バフは SG 限定・部隊 777 限定)。
  - **クルミの通常攻撃に乗る攻撃力バフ・ダメージ増加・防御デバフが一切存在せず、純粋な「フルバースト補正（+50%）」だけを単離できる編成**。

#### 突き合わせ結果

- クルミの攻撃力（スペック固定）: 基礎 79,201 + ノワール S1 バフ（ノワール攻撃力 119,896 × 0.1408 = +16,881.36）= **96,082.36**
- 基礎（防御 100 減算後）: 96,082.36 − 100 = **95,982.36**
- 素の 1 ヒット $x$（AR 武器倍率 13.65%）: 95,982.36 × 0.1365 = **13,101.59**

| 命中条件                 | 理論倍率式（加算モデル）                    | 予測（加算 +0.5） | 予測（乗算 ×1.5） | 実測値（1 ヒット） | 判定                          |
| :----------------------- | :------------------------------------------ | :---------------- | :---------------- | :----------------- | :---------------------------- |
| **通常時・非会心非コア** | $x \times 1.3$（距離）                      | **17,032**        | —                 | **17,032**         | **一致（誤差0）**             |
| **通常時・会心非コア**   | $x \times (1.3 + 0.5)$                      | **23,583**        | —                 | **23,583**         | **一致（誤差0）**             |
| **通常時・非会心コア**   | $x \times (1.3 + 1.0)$                      | **30,134**        | —                 | **30,134**         | **一致（誤差0）**             |
| **通常時・会心コア**     | $x \times (1.3 + 0.5 + 1.0)$                | **36,684**        | —                 | **36,684**         | **一致（誤差0）**             |
| **FB中・非会心非コア**   | $x \times (1.3 + \mathbf{0.5})$             | **23,583**        | 25,548            | **23,583**         | **加算モデルと一致（誤差0）** |
| **FB中・会心非コア**     | $x \times (1.3 + \mathbf{0.5} + 0.5)$       | **30,134**        | 35,374            | **30,134**         | **加算モデルと一致（誤差0）** |
| **FB中・非会心コア**     | $x \times (1.3 + \mathbf{0.5} + 1.0)$       | **36,684**        | 45,200            | **36,684**         | **加算モデルと一致（誤差0）** |
| **FB中・会心コア**       | $x \times (1.3 + \mathbf{0.5} + 0.5 + 1.0)$ | **43,235**        | 55,027            | （未確認）         | —                             |

#### 結論

1. **フルバースト補正（+50%）の計算仕様**:
   - 乗算モデル（$\times 1.5$）であれば 25,548 / 35,374 / 45,200 となるはずだが、実測された数値は 23,583 / 30,134 / 36,684 であり、**加算モデル（$1 + \text{コア} + \text{会心} + \text{距離} + \mathbf{FB}$）が 1 の位まで完全一致**した。
   - 通常時の 17,032 が消え、最低ダメージが 23,583（通常時の会心相当）に底上げされ、非会心コアが 36,684（通常時のコア会心相当）にシフトしている。
   - したがって、**通常攻撃におけるフルバースト補正は `boostTotal = 1 + コア + 会心 + 距離 + 0.5` として加算される**。Stage 4 で判明した `attackDamage`（別枠乗算）とは扱いが異なる。
2. **クルミのスキル2（ペイロード分割）の確認**:
   - フルバースト突入直後、照準中央にオレンジ色のスキルダメージ数値 **165,415** がポップアップした（フレーム 8006〜8020）。
   - クルミの S2（フルバースト中の追加ダメージ 86.17%）にコア命中倍率 2.0x が乗った理論値:
     $$95,982.36 \times 0.8617 \times 2.0 = 165,415.99$$
     実測値 165,415 と完全に一致（誤差 0.01）。
3. **ゲーム仕様上の発見（実測時の留意点）**:
   - NIKKE の仕様により、フルバーストに入ると非操作の味方全員（ノワール・デルタ・エーテル）が一斉にプレイヤーの照準位置を射撃し始める。
   - このため、HUD 総ダメージカウンターには味方の SG・SR の着弾が混ざる。1 ヒットの検証は的の上に表示されるポップアップ数値をコマ送りで読むのが最も確実。
   - 撮影手順としては、戦闘開始から「通常攻撃 1 マガジン → 即バースト → フルバースト終了（約 30〜40 秒）」で十分検証可能。

## Stage 6: スキルモデル段階 B — バースト時トリガーの持続バフ

判定基準: [design-stage6.md](design-stage6.md) 6 節。区間分割（`skills/timeline.ts`）を sim と calc で共有し、(a) 持続バフのない編成が Stage 5 と厳密一致すること、(b) 「持続時間がサイクル長以上なら常時バフと一致する」というロードマップの完了条件、(c) sim と calc の整合を自動テストで固定する。射撃場の実測は録画 14（既存）を回帰テストに取り込み、残りは未実施。

**結果（2026-09-22）: 実装・自動テスト・ブラウザ確認に加えて、射撃場の実測（録画 18・19・20・22）まで完了。** `BURST_HIT_USES_PRE_ACTIVATION_BUFFS = true` と `BURST_SKILL_FULL_BURST_BONUS = false` が実測で確定し、持続バフ・区間分割・`attackDamage` の別枠乗算がいずれも 1 の位まで一致した。残るのはクイーン（真）の分配ダメージと、バーストスキルダメージが会心するかの 2 点。 自動テストは core 20 ファイル 210 件・calc 17 件（合計 227 件、Stage 5 の 170 件から +57）。定義済みキャラは 6 体のままで、うち 4 体（ラピ・エマ：TU・マナ・クイーン（真））に `timed` を追加した。

### 自動テスト・ブラウザ確認

| 確認項目                                 | 方法                                        | 結果                                                                                                                                                                                                                               | 日付       |
| ---------------------------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| トリガーの発火フレーム                   | core `skills/__tests__/timeline.test.ts`    | 180 秒で `battleStart` = [0]、`fullBurstStart` = 600…10200（9 個）、`fullBurstEnd` = 1200…9600（8 個。最後の窓は戦闘終了で切れて発火しない）、`burstUse` は割当枠だけ 9 個・同段階の 2 体目と空枠は 0 個                           | 2026-09-22 |
| 段階の逆引き                             | 同上                                        | `assignedStepOf` が専任枠・`AllStep` の充当枠・非割当枠・空枠で正しく返り、`burstUse` の発火がそれに従う                                                                                                                           | 2026-09-22 |
| 窓の和集合（上書き延長）                 | 同上                                        | 持続 20 秒を 20 秒ごとに発火 → 窓は 1 本 `[600, 10800)`。持続 30 秒でも同じ。持続 10 秒なら 9 本。持続 0 は窓なし                                                                                                                  | 2026-09-22 |
| 窓の切り落とし                           | 同上                                        | `fullBurstEnd` の 15 秒窓が戦闘時間を越えたら `end = frames`                                                                                                                                                                       | 2026-09-22 |
| 区間の被覆と `fullBurst` の一致          | 同上                                        | 区間が `[0, frames)` を隙間・重なりなく覆う。各区間の `fullBurst` が全フレームで `isFullBurstFrame` と一致                                                                                                                         | 2026-09-22 |
| 区間数                                   | 同上                                        | 10 秒の `burstUse` バフ（窓がフルバースト窓と一致）で 18 区間。15 秒の `battleStart` を足すと境界に 900f が増える                                                                                                                  | 2026-09-22 |
| グループ化                               | 同上                                        | 枠ごとに「同じバフ状態」をまとめ、10 秒バフの 18 区間 → 2 状態（各 90 秒）。`timed` なしでも 2 状態。`key` は固定 6 桁なので丸め誤差では割れず、0.001 の差では割れる。合計が同じでも出どころが違う効果は別状態                     | 2026-09-22 |
| **退化（Stage 5 と厳密一致）**           | core `__tests__/teamTimed.test.ts`          | `timed` なしなら区間は「通常 90 秒 / FB 90 秒」の 2 つで、各区間のダメージが `秒間トリガー × 1 トリガー × 90` と **`toBe` で厳密一致**（Stage 5 の計算と同じ形）。バーストなしなら 1 区間 180 秒                                   | 2026-09-22 |
| **A: 持続 ≥ 戦闘時間 = 常時バフ**        | 同上                                        | `battleStart` × 180 秒の `timed` と、同じ値の `passive` の `totalDamage` が **厳密一致**（`burst` の ON / OFF 両方）                                                                                                               | 2026-09-22 |
| **B: 持続 ≥ サイクル長**                 | 同上                                        | `burstUse` × 20 秒なら窓は `[600, 10800)` の 1 本。区間は「バフなし通常 10 秒 / バフあり FB 90 秒 / バフあり通常 80 秒」の 3 状態で、手計算（区間ごとの レート × 秒数 × 1 トリガー）と一致。バフ稼働は 170 秒                      | 2026-09-22 |
| **C: 持続 0**                            | 同上                                        | 窓なし・定義なしと `totalDamage` が厳密一致                                                                                                                                                                                        | 2026-09-22 |
| 対象と Lv 追従                           | 同上                                        | `allies` は定義のない枠にも掛かり、`self` は発動枠だけ。Lv1 → Lv10 で比率（+10% → +55%）と維持時間（5 秒 → 14 秒）が両方追従                                                                                                       | 2026-09-22 |
| バーストヒットのスナップショット         | 同上                                        | 10 秒バフはフルバースト終了で切れるので全 9 発動が素の攻撃力基準。20 秒バフなら 1 回目だけ素（`baseHit` 900）、2 回目以降はバフ込み（1400）で、発動ごとに値が変わる                                                                | 2026-09-22 |
| **録画 14 の回帰**                       | core `__tests__/timedRegression.test.ts`    | クイーン（真）のスペック固定 119,896 で、`battleStart` の窓が `[0, 900)`、1 ペレットが 15 秒以内 **47,171.9**（会心 70,757.8）、15 秒経過後 **31,380.6**（会心 47,070.8）。録画 14 の実測 47,172 / 70,758 / 31,381 / 47,071 と一致 | 2026-09-22 |
| `timed` の解決と検証                     | core `skills/__tests__/resolve.test.ts`     | `durationRef` / `durationSeconds` はちょうど片方（両方・両方なしは `Error`）。秒 → フレームは切り上げ。`trigger` / `stat` / `casterAttack` の検証は `passive` と同じ。`burst` に `passive` は引き続き `Error`                      | 2026-09-22 |
| 定義ファイルの整合                       | core `skills/__tests__/definitions.test.ts` | 6 体: `timed` の `ref` が 100% 以下、`durationRef` が正で **Lv によらず一定**、Lv で値が単調非減少                                                                                                                                 | 2026-09-22 |
| **sim と calc の整合（厳密）**           | core `__tests__/simCalc.test.ts`            | 区間分割（`timeline.segments`）が sim と calc で同一。区間ごとの `BuffTotals` と 1 トリガー値が完全一致。バーストの発動フレームと 1 発動の内訳も一致                                                                               | 2026-09-22 |
| **sim と calc の整合（離散化誤差）**     | 同上                                        | 持続バフあり 5 体編成（180 秒）で、グループごとのトリガー数の差が「1 マガジン × 区間数」以内、枠 5% 以内、編成 3% 以内                                                                                                             | 2026-09-22 |
| **sim と calc の整合（収束）**           | 同上                                        | 180 → 1,800 → 18,000 秒で編成合計の相対差が 0.5% 以内に縮む                                                                                                                                                                        | 2026-09-22 |
| 表の各行の和が合計行と一致（`burst` ON） | ブラウザで手動（スペック固定・4 体）        | クイーン（真）322,030,046 + ラピ 65,984,987 + エマ：TU 88,908,331 + マナ 161,457,701 = 638,381,065（合計行 638,381,066 は行ごとの丸めの差 1）。DPS の和 3,546,561 は合計行と一致。CLI の calc 列とも一致                           | 2026-09-22 |
| `burst` OFF で `battleStart` だけ効く    | ブラウザで手動                              | クイーン（真）だけ 2 区間（0–15 秒 バフあり / 15–180 秒 なし）、他の 3 体は 1 区間。合計 361,952,366 = 各行の和                                                                                                                    | 2026-09-22 |
| 区間表の表示                             | ブラウザで手動                              | 時間帯・FB・攻撃力・倍率グループ・攻撃ダメージ・1 トリガー・トリガー数・ダメージ・持続バフの 9 列。クイーン（真）は 5 状態で、0–10 秒が「戦闘開始時」、20–30 秒以降が「フルバースト終了時」と出し分けられる                        | 2026-09-22 |
| 受けているバフの一覧                     | ブラウザで手動                              | 常時パッシブに続けて「戦闘開始時 → 攻撃力 +50.28%・15 秒 × 1 回」「フルバースト終了時 → …・15 秒 × 8 回」「バースト使用時 → 攻撃力 +40,040（発動者基準 40.07%）・10 秒 × 9 回」と仮定バッジが出る                                  | 2026-09-22 |

### sim と calc の差（180 秒・スペック固定・クイーン（真）/ ラピ / エマ：TU / マナ、`burst` ON）

`npm run sim -- --ids 870,10,93,290 --fixed-spec --core-hit-rate 0.6` の値。

| 枠             | 武器 | 周期 | 区間（バフ状態）の数 | 総ダメージの差 sim − calc |
| -------------- | ---- | ---- | -------------------- | ------------------------- |
| クイーン（真） | SG   | 911f | 5                    | +0.853%                   |
| ラピ           | AR   | 355f | 2                    | +0.071%                   |
| エマ：TU       | MG   | 560f | 2                    | −0.470%                   |
| マナ           | AR   | 396f | 2                    | −4.053%                   |

編成合計の差は 180 秒で −0.63%、1,800 秒で +0.083%、18,000 秒で +0.004%。

**マナの −4.05% について**: これは区間分割のモデル誤差ではなく、Stage 5 で見つけた位相の問題と同じもの。マナの周期 396f とバーストサイクル 1,200f の関係で、180 秒（27 周期）だと通常区間に 907 発・フルバースト区間に 735 発と偏る（期待値はどちらも 818 発）。1 トリガーのダメージが区間で 2.4 倍違うので、トリガー総数（1,642 対 1,636）はほぼ合っていても総ダメージが 4% ずれる。`gcd(396, 1200) = 12` は 600 を割り切る（600 / 12 = 50）ので長時間では 50% に収束し、実際 1,800 秒で +0.057%、18,000 秒で −0.041% まで縮む。**180 秒という短さでは「レート × 秒数」の期待値が数 % ずれる武器がある**というのが Stage 6 で確認できたことで、Stage 7 で動的サイクルにすると位相が揃わなくなるので消える見込み。

### 枠ごとにまとめる（グループ化）の効き方

`key` は **枠ごと**（その枠の `BuffTotals` + 効いている効果の出どころ）にしてある。編成全体で 1 つの鍵にすると、ある枠のバフ切り替え（クイーン（真）の 15 秒バフ）が他の枠の区間まで刻んでしまい、ラピのフルバースト区間が 10 秒 → 5 秒 × 2 に割れて離散化誤差が増える。枠ごとの鍵にすると、ラピ・エマ：TU・マナは 2 状態のまま（持続バフのない Stage 5 と同じ形）に保てる。

### 射撃場の実測（スペック固定・1 ヒット）

**実施（2026-09-22、録画 18・19・20・22。[captures/index.md](captures/index.md)）。** 3 分（射撃場の 03:00 モード）の `BigArms` 戦を 4 本撮った。すべてスペック固定 ON・自動バースト ON で、編成は「エーテル（I）+ デルタ（II）+ 測定対象（III）」を基本にした。

| #   | 確認したいこと                                             | 録画                | 予測                                                                    | 実測                                                              | 結果                                          |
| --- | ---------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------- |
| 1   | `burstUse` の持続バフ（ラピ・自分・attack +60.75%・10 秒） | 18                  | 通常 16,845.9 → FB+バフ 37,509.9 / 47,929.3 / 58,348.8 / 68,768.2       | 16846 / 23325 / 29804 / 36283 → **37510 / 47929 / 58349 / 68768** | **一致（4 パターンとも誤差 0）**              |
| 2   | バーストヒットが自分のバフを見るか（ラピ 657.72%）         | 18                  | 発動直前のバフなら 624,393、バフ込みなら 1,004,112、FB 込みなら 936,590 | **208,131 × 3 = 624,393**                                         | **一致（発動直前のスナップショット）**        |
| 3   | バーストスキルダメージの FB 補正（ノワール 351.64%）       | 19                  | 乗らないなら 480,611、乗るなら 720,917                                  | **480,611（読み取った 4 回すべて）**                              | **一致（乗らない。コア・距離も乗らない）**    |
| 4   | `fullBurstStart` の 2 stat 同時（マナ）                    | 20                  | 別枠乗算 87,199、加算群なら 80,439                                      | **87199 / 111422 / 135644 / 159866**                              | **一致（`attackDamage` の別枠乗算を再確認）** |
| 5   | `burstUse` × `allies` × `casterAttack`（エマ：TU）         | 22                  | デルタのコアヒットが 1.6942 倍になる                                    | 216,763 → 367,264（比 **1.6943**）                                | **一致（比での確認。下の注記）**              |
| 6   | `battleStart` / `fullBurstEnd`（クイーン（真））           | 録画 14（実測済み） | 15 秒経過で 47,172 → 31,381                                             | 47,172 / 70,758 → 31,381 / 47,071                                 | **一致（回帰テストに固定済み）**              |

#### 1〜2: ラピ（録画 18、電撃タブ）

編成はエーテル（I）+ デルタ（II）+ ラピ（III）。ラピは炎で的は電撃なので属性有利は乗らない。適正距離（25〜45m）内なので距離ボーナス +0.3 が乗る。

- **通常時**（バーストまで）: 16846 / 23325 / 29804 / 36283。予測 `(95,033 − 100) × 0.1365 × {1.3, 1.8, 2.3, 2.8}` = 16,845.9 / 23,325.0 / 29,804.2 / 36,283.4 と 1 の位まで一致。
- **フルバースト中**: 37510 / 47929 / 58349 / 68768。攻撃力が `95,033 × 1.6075 = 152,765.5` になっている（= バースト使用時の +60.75% が乗っている）。
- **バーストスキルダメージ**: 「多弾頭ミサイル」は **3 発に分かれて着弾し、1 発 208,131**。合計 624,393 = `(95,033 − 100) × 6.5772` で、**自分のバフも FB 補正もコアも距離も乗っていない**。自分のバフが乗るなら 1 発 334,704、FB が乗るなら 312,197 になるので、どちらも否定できる。モデルは「1 発動 = 624,393」の 1 ヒット扱いなので合計は一致する（分割は表示だけの違い）。
- **バフと FB の終了**: フレーム 2076 まで 37510 系が出て、2079 から 16846（バフなし・FB なし）が出る。**持続バフとフルバーストは 3 フレーム以内で同時に切れる**。「バースト使用が FB 開始より数十フレーム早いぶんだけバフが先に切れる」という想定（design-stage6.md 6.6 節の判定 B）は観測されなかったので、固定サイクルで両者を同じ窓として扱う現行モデルでよい。

#### 3: ノワール（録画 19、灼熱タブ）

バーストスキルダメージは読み取った 4 回の発動すべて **480,611**。予測は `(136,777 − 100) × 3.5164 = 480,611`。

**最終攻撃力は整数に丸められてから使われる**ことがここで分かった。ノワールのバフ後攻撃力は `119,896 × 1.1408 = 136,777.36` だが、136,777.36 で計算すると 480,612.3、136,777 で計算すると 480,611.0 になり、実測は後者。通常攻撃の 1 ペレットも 50335 / 64317（FB・距離あり）で、整数攻撃力の予測 50,335.4 / 64,317.5 と一致する（小数のままだと 50,336 / 64,318）。calc は小数のまま計算しているので 1 未満の差が出るが、実用上は無視する。

#### 4: マナ（録画 20、電撃タブ）

- **通常時（S1 の +58.08% のみ）**: 37112 / 51385 / 65659 / 79933。録画 10 と同じ値で、撮影条件が正しいことの確認になる。
- **フルバースト中（S2 の attack +63.36% と attackDamage +21.12% が乗る）**: **87199 / 111422 / 135644 / 159866**。攻撃ダメージを別枠で掛ける現行モデルの予測 87,199 と一致し、**加算グループに入れた場合の 80,439 とは 8% 離れている**。Stage 4 で訂正した「`attackDamage` は倍率グループの外」が別の文脈で再確認された。
- **2 回目のフルバーストでも再発動**: 72666 / 96888 / 121110 / 145333（距離なしの系列。同じ 1 トリガー値 48,444 から出ている）。マナの S2 は「マターシグマ状態なら」が条件で、フルバースト終了時に自分がバーストしていれば再付与される。定義ファイルの `assumes`「毎サイクル自分がバーストするのでマターシグマが再付与される」が成立している。

#### 5: エマ：TU + デルタ（録画 22、灼熱タブ、III なし）

III を入れていないのでフルバーストは起きず（R−B の暖色検出でも区間ゼロ）、エマ：TU のバースト I による持続バフだけを切り出せる編成になっている。

- デルタのコアヒットは **216,763**（バフ前）→ **367,264**（バフ中）。
- 比 367,264 / 216,763 = **1.6943**。攻撃力の比 `(63,368 + 40,040 − 100) / (63,368 − 100) = 1.6329` に、後述の環境コントロールの差（×1.078 / ×1.039 = 1.0375）を掛けると **1.6942**。**エマ：TU の発動者基準 +40,040（= 99,925 × 40.07%）が乗っていることを確認**した。
- ただし単位まで合わせられていない。エマ：TU の S1「環境コントロール」が敵に「受けるダメージ +3.9%（10 秒・再発動周期 30 秒）」を付け、さらに彼女のバーストが「環境コントロール強化（増加倍率が 100% 効果量に対し増加 = +7.8%）」を付けるためで、どちらも未対応。**この編成は `casterAttack` の単離には向かない**（design-stage6.md 8 節 5 で優先度を最低にした理由がそのまま出た）。`casterAttack` の計算自体は録画 11 でノワールの常時パッシブとして誤差 0 で確認済みなので、Stage 6 で新しい「時間制になった」部分は 1・4 で確認できている。

#### 6〜7: クイーン（真）（録画 21、灼熱タブ）

**カードの見た目からモダニアと読み違えていたのを、1 ヒットの数値で訂正した。** 47,172 / 70,758 / 94,344 は録画 12 のクイーン（真）の値そのもので、モダニア（攻撃力 120,694・武器倍率 0.0771）なら 12,087 / 16,736 / 21,385 になるはずだった。クイーン（真）の怪盗衣装は金属マスクなのでモダニアに似て見える。**キャラの同定は必ず予測値との一致で行う。**

- **`fullBurstEnd` の再付与**: フルバースト中の 1 ペレットは 47,074 / 62,766 / 78,457（= S1 の 15 秒バフが切れた後の 31,383 ベース × {1.5, 2.0, 2.5}。エーテルのデバフ込み）。**フルバーストが終わった直後から 47,172 / 70,758 に戻る** = S1 の「フルバーストタイムが終了した時、自分に攻撃力 50.28%▲（15 秒間維持）」が再付与されている。Stage 6 で新しく入れた `fullBurstEnd` トリガーが実機どおりであることの確認になる。
- **分配ダメージ**: バースト 1 回目の数値は **6,323,975**。分解すると

  ```
  (180,180 − 100) × 14.2169 × 1.30 × 1.9001 = 6,323,975
   └ S1 の戦闘開始時 +50.28%   └ 倍率 └ S2 の攻撃ダメージ +30%  └ ??
  ```

  倍率 1421.69%・攻撃ダメージ +30% は合っており、**フルバースト補正・コア・距離・会心はどれも乗っていない**（乗れば 4,992,350 や 6,656,466 になる）。`skill` 型と同じ扱いでよい。

  残る **1.9001 倍は、クイーン（真）の S2「バースト 3 段階突入時、自分に分配ダメージ 90.01%▲（10 秒間維持）」**。定義ファイルで未対応と書いてあったもので、実測で効果量が確定した。**現行の calc はクイーン（真）のバーストを 1 / 1.9001 に過小評価する。** これを扱うには「分配ダメージ▲」という専用の stat と「バースト 3 段階突入時」という専用のトリガーが要るので、段階 C（Stage 8）の語彙になる。`data/skills/870.json` の `burst` を `supported` → `partial` に落とし、notes に実測値を書いた。

#### 実測で分かった、モデルに関わる 4 点

1. **エーテルは「火力に触らない I」ではない**。S2「副反応実験」に「フルバーストタイム持続中に発動した時、同じ敵に防御力 9.38%▼（6 秒間維持）」があり、フルバースト中は敵の防御力が 100 → 90.62 に下がる。録画 18 で 37510 系と 37512 系（= 防御 90.62 の系列）が混ざって出たのはこれが原因。[design-stage5.md](design-stage5.md) 2.1 節の「火力に触らないニケ」の一覧からエーテルを外す必要がある。**今後の実測では I にエーテルを使わない。**
2. **最終攻撃力は整数に丸められてから使われる**（上記 3 節）。
3. **「受けるダメージ▲」は別枠の乗数らしい**。録画 22 のデルタのコアヒットは `104,313 × 2 × 1.039 = 216,762`（実測 216,763）で、加算グループに入れた `104,313 × (2 + 0.039) = 212,694` ではない。[requirements.md](requirements.md) 5.1 の式は被ダメ増加デバフを加算グループに入れているので、実際に扱う段階（Stage 8 以降）で再確認が要る。
4. **3 分の実戦では的が反撃するので、被弾トリガーのスキルが発動する**。ラピの S1「N 回攻撃を受けた時、自分に攻撃力 21.81%▲（20 秒）」が 3 回目のバースト以降に乗っており、録画 18 の後半の値は `95,033 × (1 + 0.6075 + 0.2181)` 基準になっていた（35504 / 47339 など）。**スキルの切り出しに使えるのは 1 回目のフルバーストまで。**

#### 未確定のまま残るもの

- **バーストスキルダメージが会心するか**。ラピ 3 発 + ノワール 4 回 = 7 ヒットがすべて非会心の値だった。会心率 15% なら 7 回とも非会心になる確率は 32% なので、これでは決着しない。現行モデルは会心期待値（×1.075）を掛けている。発動回数を増やすか、会心率を上げた編成で撮り直す必要がある。
- ~~**クイーン（真）の分配ダメージ**~~ → 録画 21 で実測済み（上記 7）。ただし分配ダメージ▲（段階 C の語彙）が未対応なので、**calc の値は実機の 1 / 1.9001**。

## Stage 7: バーストゲージ蓄積と動的サイクル

判定基準: [design-stage7.md](design-stage7.md) 6 節。時刻表を「発動 1 回ずつ」の形（`BurstSchedule`）に一般化し、(a) 固定サイクル（`burstModel: 'fixed'`）で Stage 6 と厳密一致すること、(b) 状態機械（ゲージ・チェーン・CT・タイムアウト）の挙動、(c) sim と calc の整合、(d) 録画 18〜22 の回数・周期の再現を自動テストで固定する。ゲージ量の較正は新規撮影（設計書 6.5）で行う。

**結果（2026-09-23）: 実装・自動テスト・ブラウザ確認に加えて、単騎の録画 13 本（録画 23〜35）でゲージ量を較正した。** 録画 18〜21 のフルバースト 5 回・40 秒周期と、録画 22 の「III なしでチェーン失敗」を再現する。1 回目の満タンまでの時間は、較正前の −167f〜+163f から −37f〜+61f に縮んだ。自動テストは core 23 ファイル 253 件・calc 19 件（合計 272 件、Stage 6 の 227 件から +45）。

### 自動テスト・ブラウザ確認

| 確認項目                               | 方法                                           | 結果                                                                                                                                                                                                       | 日付       |
| -------------------------------------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **退化（Stage 6 と厳密一致）**         | Stage 5 / 6 の既存テスト全部                   | 時刻表を `BurstSchedule` に書き換えたあとも、`burstModel: 'fixed'` で既存 228 件が**数値を変えずに**通る                                                                                                   | 2026-09-23 |
| データの取り込み                       | core `scripts/normalize.test.ts`               | `skill_cooltime / 100`、`full_charge_burst_energy` の 0 → 1・`/ 10000`、`change_burst_step` の検証。203 ファイルの再生成で既存フィールドの変更は末尾カンマだけ                                             | 2026-09-23 |
| ゲージ律速                             | core `burst/__tests__/controller.test.ts`      | 200f で満タン → 20f 後に I、さらに 20f ずつ II・III、フルバースト 600f、終了後に 0 から溜め直す（フルバースト中は溜まらない）                                                                              | 2026-09-23 |
| CT 律速                                | 同上                                           | 全員 CT 40 秒ならフルバースト開始がちょうど 2,400f 間隔で 180 秒に 5 回。満タン後に I の CT を待つ間はタイムアウトしない                                                                                   | 2026-09-23 |
| 同じ段階の 2 体                        | 同上                                           | III 2 体（CT 40 秒）+ I / II（CT 20 秒）で III が交互に撃ち、周期が 20 秒に縮む。候補は枠番号の若い順                                                                                                      | 2026-09-23 |
| 段階の欠け・II の CT 待ち              | 同上                                           | III なしは II の 600f 後にチェーン失敗・ゲージ 0。I（CT 20 秒）が撃ったあと II（CT 40 秒）が明けないときも 600f で失敗（録画 22 の 2 回目と同じ）                                                          | 2026-09-23 |
| リエントリー・`AllStep`                | 同上                                           | `Step1 → Step1` のニケがいると I が 2 回撃ってから II。`AllStep`（`NextStep`）は専任のいない段階を埋める                                                                                                   | 2026-09-23 |
| ゲージ量                               | core `burst/__tests__/dynamic.test.ts`         | ラピ 4,000 / ノワール 90,000（9,000 × 10 ペレット）/ エーテル 40,000 / デルタ 132,500（53,000 × 2.5）/ エマ：TU 1,000。チャージなし武器は倍率を無視。CT 40 秒 → 2,400f                                     | 2026-09-23 |
| 射撃は時刻表に依存しない               | core `sim/__tests__/engine.test.ts`            | `burst` の ON / OFF で sim の射撃フレーム列が一致（2 パスで済む根拠）。trace に `gaugeFull` と I → II → III（20f 間隔）・フルバースト開始 / 終了が出る                                                     | 2026-09-23 |
| **sim と calc の整合（動的サイクル）** | core `__tests__/simCalc.test.ts`               | 時刻表・区間分割・区間ごとの 1 トリガー値・バーストの発動が sim と calc で同一。枠 5%・編成 3% 以内、18,000 秒で 0.5% 以内。全員 CT 40 秒の 5 体編成は固定サイクル（9 回）より少なく、平均サイクル ≥ 40 秒 | 2026-09-23 |
| **録画 18〜22 の回帰**                 | core `__tests__/burstRegression.test.ts`       | 18・19・20・21: 5 回・間隔 2,400f・チェーン失敗 0・稼働率 5/18・I → II → III が枠順。22: フルバースト 0 回、II の 600f 後に失敗。1 回目の満タンは未較正なので ±200f                                        | 2026-09-23 |
| 表示                                   | ブラウザで手動（録画 18 の編成・スペック固定） | 「フルバースト 5 回（稼働率 27.8%・平均サイクル 40 秒・初回 6.5 秒）」、時刻表 15 行（III の行にバーストスキル 671,223 とフルバースト区間 6.5–16.5s など）。録画 22 の編成は「III のニケがいない」と出る   | 2026-09-23 |

### 固定サイクルとの差（180 秒・スペック固定・`--core-hit-rate 0`）

| 録画の編成 | フルバースト | 初回（秒） | 稼働率 | チェーン失敗 | calc 動的   | calc 固定   | 動的 / 固定 | sim − calc（動的） |
| ---------- | ------------ | ---------- | ------ | ------------ | ----------- | ----------- | ----------- | ------------------ |
| 18         | 5            | 6.47       | 27.8%  | 0            | 88,818,505  | 103,784,631 | −14.4%      | +0.18%             |
| 19         | 5            | 4.33       | 27.8%  | 0            | 157,648,750 | 173,909,017 | −9.3%       | +0.31%             |
| 20         | 5            | 5.67       | 27.8%  | 0            | 121,404,806 | 142,608,140 | −14.9%      | +1.29%             |
| 21         | 5            | 5.67       | 27.8%  | 0            | 182,430,289 | 238,460,530 | −23.5%      | −0.09%             |
| 22         | 0            | —          | 0%     | 9            | 59,855,617  | 75,865,102  | −21.1%      | −0.69%             |

録画 21 の差が大きいのは、クイーン（真）の `fullBurstEnd` 15 秒バフの回数が 8 回 → 4 回に減るため。録画 22 は固定サイクルが「III がいなくてもフルバーストは起きる」と仮定していたぶんが消えた。

### ゲージの読み取り（`tools/captures/gauge.ts`、2026-09-23）

BURST バー（x 1793〜1905、y 442、113px）の充填率を 1 フレームずつ読んだ。満タンでも右端の 1px は暗いので、112px（99.1%）で満タンとみなす。

**1 回目の満タンまで**（最初にゲージが増えたフレームから）:

| 録画 | 操作キャラ     | 実測 | calc（`target` × ペレット × チャージ倍率） | 差    |
| ---- | -------------- | ---- | ------------------------------------------ | ----- |
| 18   | ラピ（AR）     | 427f | 328f                                       | −99f  |
| 19   | ノワール（SG） | 266f | 200f                                       | −66f  |
| 20   | マナ（AR）     | 267f | 280f                                       | +13f  |
| 21   | クイーン（SG） | 447f | 280f                                       | −167f |
| 22   | デルタ（SR）   | 329f | 492f                                       | +163f |

符号がばらばらなので、上限の値（1,000,000）を 1 つ直すだけでは合わない。録画 20 はマナ S2 の「バーストゲージのチャージ速度▲」（戦闘開始から持続。未対応）が乗っているので比較から外す。

**1 発ずつの増分**（1 フレームで跳ねた量。1px ≈ 0.88%）:

| 録画       | 撃ったニケ（操作 / AI）                        | 実測                | `target` の予測 | `burst`（半分）の予測                     |
| ---------- | ---------------------------------------------- | ------------------- | --------------- | ----------------------------------------- |
| 22         | デルタ（操作・フルチャージ）                   | **15.9%**（4 回）   | 13.25%          | 6.6%                                      |
| 19・21・18 | デルタ（AI）                                   | **6.2%**（各 4 回） | 13.25%          | 6.6%                                      |
| 19         | ノワール（操作）+ エーテル（AI）、同じフレーム | **約 11%**          | 9 + 4 = 13%     | 9 + 2 = 11%（操作 `target` + AI `burst`） |
| 21         | クイーン（操作）+ エーテル（AI）               | **6.2〜8.0%**       | 4 + 4 = 8%      | 4 + 2 = 6%                                |
| 22         | エマ：TU（AI・MG）の秒あたり                   | **約 7.6%/秒**      | 7.0%/秒         | 3.5%/秒                                   |

- デルタの照準画面に「CHARGE 250%」と並んで「**BURST 250%**」が出る（録画 22）。`full_charge_burst_energy`（25,000）がゲージのチャージ倍率 ×2.5 であることは画面の表示で確かめられた。
- **AI のデルタとエーテルは `burst_energy_pershot`（`target` の半分）で説明できる**が、AI のエマ：TU は `target` どおり、操作中のデルタは `target × 2.5` よりさらに 2 割多い。「操作キャラは `target`、AI は `burst`」だけでは説明がつかない。コアに当たったか（操作キャラはコアを狙っている）が効いている可能性もある。
- 固定の時刻表（CT 40 秒）で回る録画 18〜21 では、この差は 1 回目のフルバーストの時刻（数秒）にしか効かない。2 回目以降は CT で決まるので、回数と周期は合っている。

この読み取りで「操作 / AI で違う」と見当を付け、単騎で撮り直して較正した（次の節）。上の表の予測はすべて較正前の式。

### 単騎の録画によるゲージの較正（録画 23〜35、2026-09-23）

同じキャラを「操作してコアを狙う / 操作して胴体を撃つ / AI にする」の 3 通りで撮った（AI のときは I-DOLL を操作し、撃たせていない。名前パネルで与ダメ 0 を確認）。1px ≈ 0.885%。AR・MG は 1 発が 1px 未満なので、1 マガジンの伸びを弾数で割った。

| 録画   | ニケ（武器）   | 条件               | 1 トリガーの実測                      | CDN の `target` × ペレット × チャージ | 比                           |
| ------ | -------------- | ------------------ | ------------------------------------- | ------------------------------------- | ---------------------------- |
| 28・29 | ラピ（AR）     | 操作（コア・胴体） | 32〜33px / 60 発 = **0.47〜0.49%**    | 0.40%                                 | **×1.18〜1.22**              |
| 30     | ラピ（AR）     | AI                 | 同上                                  | 0.40%                                 | ×1.18〜1.22                  |
| 31・34 | エマ（MG）     | 操作（コア・胴体） | 40〜41px / 300 発 = **0.118〜0.121%** | 0.10%                                 | **×1.18〜1.21**              |
| 35     | エマ（MG）     | AI                 | 同上                                  | 0.10%                                 | ×1.18〜1.21                  |
| 32・33 | ベロータ（RL） | 操作（コア・胴体） | 13px = **11.5%**                      | 2.8% × 3.5 = 9.8%                     | **×1.13〜1.22**              |
| 23・24 | デルタ（SR）   | 操作（コア・胴体） | 17〜18px = **15.0〜15.9%**            | 5.3% × 2.5 = 13.25%                   | **×1.13〜1.24**              |
| 25     | デルタ（SR）   | **AI**（82f ごと） | 7〜8px = **6.2〜7.1%**                | 5.3%（× 2.5 なら 13.25%）             | ×1.2（**チャージ倍率なし**） |
| 26     | ノワール（SG） | 操作               | 平均 **8.1%**（5.3〜10.6%）           | 0.9% × 10 = 9%                        | ×0.90                        |
| 27     | ノワール（SG） | AI                 | 平均 **8.4%**（6.2〜10.6%）           | 9%                                    | ×0.93                        |

分かったこと:

1. **コアと胴体で差はない**（ラピ・エマ・ベロータ・デルタとも同じ値）。
2. **どの武器も CDN の `target_burst_energy_pershot` の 1.2 倍**溜まる（読み取りの幅で 1.17〜1.21 に重なる）。`BURST_ENERGY_MULTIPLIER = 1.2` にした。射撃場の的（BigArms）固有の値かもしれない（レイドボスでは未確認）。
3. **フルチャージ倍率は操作キャラにだけ乗る**。AI のデルタは 82f ごと（フルチャージの間隔）に撃っているのに 1 発 6.2〜7.1% で、倍率なし（53,000 × 1.2 = 6.36%）と一致する。操作中は 15.9%（53,000 × 1.2 × 2.5）。AI / 操作の差が出るのはチャージ武器だけで、AR・MG・SG は同じ。`TeamInput.controlledSlot` を足し、省略時は全員 AI 扱いにした。
4. **SG はペレットの一部しかゲージにならない**。ノワールの 1 トリガー 8.1〜8.4% は、全ペレット命中の予測 10.8%（9% × 1.2）の 0.75〜0.78 倍。`SG_PELLET_GAUGE_HIT_RATE = 0.75` にした。**ダメージの計算は全ペレット命中のまま**（Stage 2 以来の仮定）なので、射撃場の距離で 1/4 のペレットが外れているなら SG の総ダメージは過大になっている可能性がある（Stage 8 以降で確認する）。
5. `burst_energy_pershot`（`target` の半分）で説明できる読み取りは無かった。Stage 7 では使わないまま保持する。
6. ベロータの録画では、1 発の直後に +15〜35% の別の跳ねが入る区間がある（S1・S2 の追加攻撃かもしれない）。1 発の値は跳ねの無い区間から読んだ。

**録画 18〜22 の 1 回目の満タンまで（較正後）**:

| 録画 | 操作キャラ      | 実測 | 較正前 | 較正後 | 差   |
| ---- | --------------- | ---- | ------ | ------ | ---- |
| 18   | ラピ（III）     | 427f | 328f   | 425f   | −2f  |
| 19   | ノワール（III） | 266f | 200f   | 246f   | −20f |
| 21   | クイーン（III） | 447f | 280f   | 410f   | −37f |
| 22   | デルタ（II）    | 329f | 492f   | 390f   | +61f |

録画 20 はマナのゲージ速度バフ（未対応）が乗るので除いた。録画 22 だけ +61f 残る。MG のスピンアップ（エマ：TU）で最初の数十発の間隔が実機より長いのかもしれない。回帰テストは 18・19・21 を ±40f、22 を ±70f で固定した（設計書 6.7 の目標 ±30f には届いていない）。

### 未確定のまま残るもの

- ×1.2 が射撃場の的だけのものか、敵ごとに違うか（レイドボスでは未確認）。
- 自動戦闘（オート）のときに「操作キャラ」扱いになるか。calc の既定は全員 AI（フルチャージ倍率なし）。
- SG のダメージ側のペレット命中（ゲージでは 75%）。ダメージは全ペレット命中のまま。
- 段階の演出時間（満タン → I、I → II、II → III）。初期値は 20f ずつ。満タンから「バーが消える」（I のアイコンが出る）までが 10〜82f とばらつく。自動バーストが射撃中・リロード中を待っている可能性がある。
- 録画 22 の 1 回目の満タンが +61f 残る（MG のスピンアップか）。
- `burst_duration` の 500 / 1,500（ミハラ・イサベル・ベスティー / モダニア）の意味。

## Stage 8: スキルモデル段階 C — 回数トリガーと倍率ダメージ

判定基準: [design-stage8.md](design-stage8.md) 8 節。(a) Stage 8 の語彙を使わない編成で Stage 7 と厳密一致すること、(b) 射撃の回数・発動の回数・段階突入のトリガーの発火フレーム、(c) sim と calc の整合（倍率ダメージは厳密一致）、(d) 録画 20・21 の回帰を自動テストで固定する。仕組みの実機との照合はドレイク・イサベルの新規撮影（設計書 8.6 節）で行う。

**結果（2026-09-23）: 実装・自動テスト・ブラウザ確認に加えて、射撃場の撮影 3 本（録画 36〜38）で 8.6 節の 5 項目を確かめた。** 倍率ダメージにフルバースト補正 +0.5 が乗る（仮定を訂正）、イサベルのバーストは発動前の段階を見る（仮定を訂正）、の 2 点を直した。自動テストは core 26 ファイル 308 件・calc 19 件（合計 327 件、Stage 7 の 272 件から +55）。

### 自動テスト・ブラウザ確認

| 確認項目                          | 方法                                   | 結果                                                                                                                                                                                                                                                                                                                                          | 日付       |
| --------------------------------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **退化（Stage 7 と厳密一致）**    | Stage 7 までの既存テスト全部           | 1 パス目を `planTeamRun`（射撃の列 → 時刻表 → 区間 → 倍率ダメージ）にまとめ、sim の射撃を射撃の列から読むようにしたあとも、既存 272 件が**数値を変えずに**通る（変えたのは型の形の期待値 3 件だけ）                                                                                                                                           | 2026-09-23 |
| DSL の検証                        | core `skills/__tests__/stage8.test.ts` | 回数トリガーの `every` / `everyRef` の排他・範囲・未知のフィールド、`atLeast ≥ 1`、毎回の `damage` はエラー、`burstGaugeSpeed` を `timed` に書くとエラー                                                                                                                                                                                      | 2026-09-23 |
| 射撃の回数                        | 同上                                   | 9 発ごとにリロードする射撃の列で `every: 10` が 10・20 発目に発火（リロードをまたいでも数える）。バフ窓は次のフレームから。`fullChargeShot` はチャージ武器だけ                                                                                                                                                                                | 2026-09-23 |
| 発動の回数                        | 同上                                   | `atLeast: 3` が 3 回目以降の発動のたびに発火。`fullBurstStart` は編成全体の回数                                                                                                                                                                                                                                                               | 2026-09-23 |
| 段階突入・フルバースト時間        | core `burst/__tests__/stage8.test.ts`  | 段階 2 / 3 は I / II の発動フレーム、段階 1 は満タンとリエントリー、固定サイクルは 3 つとも発動フレーム。フルバーストは StepFull を撃った枠の長さ（イサベル 300f・モダニア 900f・他 600f）                                                                                                                                                    | 2026-09-23 |
| ゲージ速度                        | 同上                                   | `gaugeSpeed` 0 なら Stage 7 と同じ時刻表。マナの枠に 0.704 で 1 回目の満タンが早まる。射撃の列を渡しても自分で回しても同じ時刻表                                                                                                                                                                                                              | 2026-09-23 |
| 分配ダメージ▲                     | 同上                                   | `distributedDamage` は分配ダメージの効果にだけ掛かる（skill / additional には掛からない）                                                                                                                                                                                                                                                     | 2026-09-23 |
| **sim と calc の整合（Stage 8）** | core `__tests__/stage8Team.test.ts`    | エーテル + デルタ + イサベル + ドレイク + クイーン（真）で、射撃の列・時刻表・区間・倍率ダメージの発動列が一致、倍率ダメージの合計も一致。通常攻撃を含む総ダメージは編成 +0.33%・枠 5% 以内                                                                                                                                                   | 2026-09-23 |
| **録画 21 の回帰**                | 同上                                   | クイーン（真）の 1 回目のバースト（非会心）が **6,323,965.6**（実測 6,323,975。差 −9 は最終攻撃力の丸め 180,179.7 → 180,180 のぶん）。分配ダメージ ×1.9001、攻撃ダメージ ×1.30                                                                                                                                                                | 2026-09-23 |
| **録画 20 の回帰**                | 同上                                   | マナのゲージ速度 70.4% を入れて、1 回目の満タンが **270f**（実測 267f。ゲージ速度なしでは 411f）。フルバーストは 5 回・40 秒周期のまま                                                                                                                                                                                                        | 2026-09-23 |
| 表示                              | ブラウザで手動（スペック固定）         | 録画 21 の編成で「分配ダメージ（バフ）×1.9001」、1 回目のバースト 6,798,263（会心期待値 ×1.075 込み）。イサベル + ドレイクの編成でフルバースト 5 秒・稼働率 13.9%、「スキルダメージ」列、倍率ダメージの内訳（「通常攻撃 10 回ごと」× 23 回、「バースト使用 2 回目以降」× 4 回）、持続バフのラベル「バースト使用 3 回目以降 → 攻撃力 +17.28%」 | 2026-09-23 |

### 数値への影響（180 秒・スペック固定・`--core-hit-rate 0` 相当）

| 編成                                          | 変わったところ                                                                                                                                |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 録画 21（エーテル + デルタ + クイーン（真）） | バースト 1 回目が ×1.9001。2 回目以降もバースト 3 段階突入時の 10 秒バフが乗る                                                                |
| 録画 20（エーテル + デルタ + マナ）           | 1 回目のフルバーストが 約 2.3 秒早まる。回数・周期は変わらない                                                                                |
| エーテル + デルタ + イサベル + ドレイク       | フルバースト 5 回・各 5 秒（稼働率 13.9%）。イサベルの段階別追加ダメージ 7 回 3,398,874、ドレイクの S2 23 回 3,024,370。sim − calc は +0.115% |
| 上記以外の既存編成                            | 変わらない（退化テスト）                                                                                                                      |

### 射撃場の実測（録画 36〜38、2026-09-23）

3 本ともスペック固定・3 分モード（的が反撃する）・灼熱の BigArms（属性有利なし・防御 100）。キャラは戦闘後の名前パネルで確認した。

| 録画 | 編成                                     | 操作     | 尺      |
| ---- | ---------------------------------------- | -------- | ------- |
| 36   | ドレイク単騎（宝物あり）                 | ドレイク | 81.9 s  |
| 37   | ラム（I）+ デルタ（II）+ ドレイク（III） | ドレイク | 115.0 s |
| 38   | ラム（I）+ デルタ（II）+ イサベル（III） | イサベル | 208.5 s |

**注意: ユーザーのドレイクは宝物（お気に入りアイテム）を持っていて、射撃場のスペック固定でも宝物によるスキル変更が反映される。** Blablalink の CDN データ（`packages/core/data/characters/`）は宝物なしの基礎スキルなので、録画 36・37 の読み取りには宝物版の説明文（ユーザーが Blablalink の画面から提供）を使った。宝物版の違い: S1 に「ショットガンを所持する味方に 攻撃力 63.88%・最大装弾数 50.14%（10 秒）」、S2 に「5 回攻撃した時 最終攻撃力の 201.6% のダメージ」、バーストが 3009.6% + 自分に攻撃ダメージ 31.68%（10 秒）。

| #   | 確認したいこと             | 結果                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 射撃の回数トリガー         | ペレット 25,672 / 38,508（会心）/ 51,345（コア）が予測と一致。S2「10 回攻撃」の **118,059**（= 119,796 × 98.55%、非会心）が 10 発目（装弾数 9 のリロード後の 1 発目）に出た → **リロードをまたいで数える**。宝物版の「5 回攻撃 201.6%」の 241,509 は 5・15 発目…、10 発目には両方出る。途中で距離ボーナス（ペレット 33,374 / 46,210 = ×1.3 / ×1.8）が付いても S2 は 118,059 のまま → **倍率ダメージに距離ボーナスは乗らない** |
| 2   | 倍率ダメージとフルバースト | フルバースト中のペレット 89,141 / 118,855（比 1.5 : 2.0）、S2 **409,932**（白・非会心）、宝物版の 5 回攻撃 838,582。**S2 / ペレットの比がフルバーストの外（118,059 / 25,672）と中（409,932 / 89,141）で同じ 4.5987** → 倍率ダメージにも**フルバースト補正 +0.5 が乗る**。攻撃ダメージ（宝物版バーストの +31.68%）も乗る（89,141 = 25,672 × 1.75793 × 1.3168 × 1.5）                                                           |
| 3   | 発動回数の段階             | 段階 2（クリダメ +18.03%）は 2 回目の発動から（フルバースト中の会心 / 非会心が 88,206 / 64,013 = (1.8 + 0.5 + 0.1803) / 1.8）。段階 3（攻撃力 +17.28%）は 3 回目の発動から（62,569 / 90,947）。5 回目まで付き直す                                                                                                                                                                                                             |
| 4   | 段階別の追加ダメージの順序 | **段階 2 の追加ダメージは 3 回目、段階 3 は 4 回目から**（758,766 = 299.7% × 1.5 × 1.3996、1,509,234 = 349.65% × 段階 3 の攻撃力 × (2.0 + 0.1803)（会心）× 1.3996）。バーストの段階別の効果は、**同じ発動で S1 が上げる前の段階**を見る（設計書 2.4 節の仮定を訂正）。段階 1 の「受けるダメージ 39.96%▲（5 秒）」も 2 回目から出る                                                                                            |
| 5   | フルバースト時間（5 秒）   | カウントダウンが 04.80（f1500）→ 00.00（f1788）など、5 回とも **300f**。イサベルの `burst_duration` 500 どおり                                                                                                                                                                                                                                                                                                                |

#### 実測で分かった、そのほかのこと

- **追加ダメージは会心する**（イサベルの段階 3 の追加ダメージがオレンジ 1,509,234。段階 2 のクリダメ +18.03% も乗る）。ドレイクの S2 は録画の範囲で会心が 1 回も出なかった。
- **受けるダメージ▲（敵デバフ）は別枠の乗数**（×1.3996 = 1 + 39.96%）。ペレットと追加ダメージに掛かり、同じ発動のバーストスキルダメージ（180,710）には掛からない。敵デバフは語彙にないので未対応（イサベル S1 の notes に記録）。verification.md Stage 4 節の「被ダメ増加は別枠の乗数らしい」を裏付ける。
- **イサベルのバーストスキルダメージは発動から約 2.5 秒遅れて着弾する**（フルバースト開始 f1488 → 180,710 が f1632）。S1 の段階のバフもこの着弾のころから効く（3 回目の発動後、着弾までのペレットは段階 3 の攻撃力なし）。モデルは発動フレームで付けているので、この 2.5 秒ぶんだけ早い。
- バーストスキルダメージ 180,710 は 1〜3 回目、211,963 は 4 回目以降（段階 3 の攻撃力が発動前から付いている）。発動直前のバフで計算する既存の規則どおり。
- ドレイク（宝物版）のバースト 3009.6% は 1,201,793（= 119,796 × 10.032 = 3009.6% ÷ 3）の表示だった。→ Stage 9 で録画 37 を読み直し、3 回に分けて当たる（1,201,793 × 3）と確認した（Stage 9 節）。

## Stage 9: 宝物（お気に入りアイテム）版スキル

判定基準: [design-stage9.md](design-stage9.md) 8 節。(a) 宝物の段階 0（既定）で Stage 8 と厳密一致すること、(b) SSR 21 体の宝物版スキルの取り込み、(c) ドレイク段階 3 で録画 36・37 の数値と一致すること、(d) calc で段階を選べること。新規撮影なし。

**結果（2026-09-23）: 4 項目とも確認した。** 録画 37 の読み直しで、ドレイクの宝物版バースト 3009.6% は 1,201,793 × 3 回に分けて当たると分かった（合計は 1 回として扱うモデルと一致）。自動テストは core 28 ファイル 354 件・calc 22 件（合計 376 件、Stage 8 の 327 件から +49）。

### 自動テスト・ブラウザ確認

| 確認項目                       | 方法                                                          | 結果                                                                                                                                                                                                                                                                                                                                                                                    | 日付       |
| ------------------------------ | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **退化（Stage 8 と厳密一致）** | Stage 8 までの既存テスト全部                                  | 宝物の段階を省略（0）した既存 327 件が**数値を変えずに**通る（変えたのはテスト用キャラの `treasure: null` と `isEffectTarget` の引数の形だけ）                                                                                                                                                                                                                                          | 2026-09-23 |
| 取り込み                       | core `scripts/normalize.test.ts`・`blablalink/path.test.ts`   | 配列順が解放順、スキル ID の下 6 桁 = 基礎版 + 50 の照合、スロットが 3 つでない・ja / en の並びが違う・`name_code` が 2 体に当たる場合はエラー。`favorite_rare_map.json` と `favorite_200801.json` の難読化パスが CDN で 200 を返すもの                                                                                                                                                 | 2026-09-23 |
| データ                         | `npm run fetch-data`・core `__tests__/stage9Treasure.test.ts` | 202 体中、宝物を持つのは 21 体（設計書 0.3 節の一覧どおり）。ほかは `"treasure": null` が増えただけ。ドレイクの Lv10 値が Blablalink の画面と一致                                                                                                                                                                                                                                       | 2026-09-23 |
| 差し替え                       | core `skills/__tests__/treasure.test.ts`                      | 段階 0 は同じオブジェクト、段階 N は解放順の先頭 N 個だけ、定義の無いスロットは unsupported。凍結した引数を書き換えない。`applyTreasureToTeam` は 2 回通しても同じで、`planTeamRun` を直接呼んでも効く                                                                                                                                                                                  | 2026-09-23 |
| 語彙                           | 同上・`skills/__tests__/targets.test.ts`                      | `targetWeapon` は `allies` とだけ組める、解決済みの効果に写る、武器種の一致する枠にだけ掛かる。`treasureSkills` の未知のスロットはエラー                                                                                                                                                                                                                                                | 2026-09-23 |
| 定義                           | core `skills/__tests__/definitions.test.ts`                   | ドレイクの宝物版（段階 3 で差し替えたもの）にも基礎版と同じ検査（ref の範囲・回数と秒数が Lv で変わらない・未対応の説明）                                                                                                                                                                                                                                                               | 2026-09-23 |
| sim と calc の整合             | core `__tests__/stage9Treasure.test.ts`                       | 録画 37 の編成（ドレイク段階 3）で、倍率ダメージの発動列と合計が厳密一致、総ダメージは 3% 以内                                                                                                                                                                                                                                                                                          | 2026-09-23 |
| calc の状態                    | calc `team.test.ts`                                           | 既定 0、キャラを変える・枠を空けると 0、スペック固定でも上書きしない、Stage 8 までの保存データは 0、0..3 以外は復元しない                                                                                                                                                                                                                                                               | 2026-09-23 |
| 表示                           | ブラウザで手動                                                | エーテル + デルタ + イサベル + ドレイクの編成で、ドレイクだけに「宝物: なし / 1 段階（スキル 1）/ 2 段階（+スキル 2）/ 3 段階（+バースト）」が出る。3 段階で 3 スキルに「宝物版」のバッジ、ドレイクの総ダメージ 75,598,469 → 107,081,911。「攻撃力 +63.88%（SG の味方）」はエーテル・イサベル・ドレイクにだけ付き、デルタ（SR）には付かない。再読み込み後も段階が残る。0 に戻すと元の値 | 2026-09-23 |

### 録画 36・37 の回帰（ドレイク段階 3、新規撮影なし）

| 項目                               | モデル                                                                            | 実測                      |
| ---------------------------------- | --------------------------------------------------------------------------------- | ------------------------- |
| S2「5 回攻撃」（フルバーストの外） | 119,796 × 201.6% = 241,509                                                        | 241,509（録画 36）        |
| S2「5 回攻撃」（フルバースト中）   | 210,593 × 201.6% × 1.5 × 1.3168 = 838,583（丸めない攻撃力 210,593.24 のぶん +1）  | 838,582（録画 37）        |
| S2「10 回攻撃」（フルバースト中）  | 210,593 × 98.55% × 1.5 × 1.3168 = 409,932                                         | 409,932（録画 37）        |
| フルバースト中のペレット           | 210,593 × 21.43% × 1.5 × 1.3168 = 89,141                                          | 89,141（録画 37）         |
| SG 向けの攻撃力 +63.88%            | ドレイクにだけ（ラム・デルタは SR なので +11.85% だけ）                           | 録画からは未確認（下記）  |
| バースト 3009.6%                   | 119,796 × 3009.6% = 3,605,381（発動前のバフ。攻撃ダメージ・フルバースト補正なし） | 1,201,793 × 3 = 3,605,379 |

- **ドレイクの宝物版バーストは 3 回に分けて当たる。** 録画 37 の総ダメージ表示が f1286・f1314・f1342（28f おき）に 1,201,793 ずつ増えた（`plan/captures/frames/20260923-37_…_f1250-1366_burst3hits.jpg`）。1 回あたり 1003.2% で、合計が説明文の 3009.6%。単体ボスでは合計だけが効くので、モデルは 1 回として扱ったまま（`101.json` の assumes に記録）。Stage 8 の verification に残っていた「1/3 の値で表示された」件はこれで解決。
- SG 向けの攻撃力がラム・デルタに乗っていないことは、録画 37 のラム・デルタのダメージ数値を読んでいないので**未確認**。説明文どおり「ショットガンを所持する味方」として実装した。
- 宝物のステータス（ドレイクは攻撃力 9,688）は、スペック固定では乗らない前提のまま（録画 36 の 119,796 が宝物なしの計算値と一致）。

### 数値への影響（180 秒・スペック固定・`--core-hit-rate 0`・灼熱の的）

| 編成                                              | 段階 0（Stage 8 と同じ） | ドレイク段階 3            | 変わったところ                                                                                                                                          |
| ------------------------------------------------- | ------------------------ | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 録画 37（ラム + デルタ + ドレイク、ドレイク操作） | 編成 144,059,710         | 編成 215,958,055（×1.50） | ドレイク 107,540,132 → 179,438,477。バースト 1 回 1,614,910 → 3,875,784（×2.4）、倍率ダメージ 23 回 3,567,665 → 70 回 26,241,824。sim − calc は −0.176% |
| エーテル + デルタ + イサベル + ドレイク           | 編成 258,585,342         | 編成 319,089,278（×1.23） | SG の味方（エーテル・イサベル・ドレイク）にフルバースト中の攻撃力 +63.88%、ドレイクの倍率ダメージが 5 回ごとに増える。sim − calc は +0.097%             |

- 最大装弾数▲（S1 の SG 向け 50.14%、バーストの自分 72.18%）は Stage 10 の語彙なので入っていない。実機ではフルバースト中の射撃の回数（と S2 の発動回数）がもう少し多い。

## Stage 10: スキルモデル段階 D — 射撃が変わるバフ・CT 短縮・最後の弾丸

判定基準: [design-stage10.md](design-stage10.md) 8 節。(a) 射撃に効く効果の無い編成で Stage 9 と厳密一致すること、(b) 1 パス目のフレームループが作る窓と planBuffTimeline の窓が一致すること、(c) 既存の録画 19・37 の弾数表示を再現すること、(d) sim と calc が厳密一致する量と許容差、(e) 射撃場の新規撮影（録画 A・B）で仮の定数を確定すること。

**結果（2026-09-23）: (a)〜(d) を確認した。(e) の撮影はこれから。** 自動テストは core 32 ファイル 431 件・calc 22 件（合計 453 件、Stage 9 の 376 件から +77）。

### 自動テスト・ブラウザ確認

| 確認項目                       | 方法                                                 | 結果                                                                                                                                                                                                                                                                                                                                          | 日付       |
| ------------------------------ | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **退化（Stage 9 と厳密一致）** | Stage 9 までの既存テスト全部                         | 1 パス目をフレームループに、分割リロードを 1 回分ずつに、トリガーの判定を `TriggerTracker` にしたあとも、既存 376 件が**数値を変えずに**通る（変えたのはテスト用のバフ合計に新しい 4 フィールドを足しただけ）                                                                                                                                 | 2026-09-23 |
| 射手の退化                     | core `sim/__tests__/stage10Shooter.test.ts`          | 全 202 体で、基礎値の射手の 180 秒の発射フレーム列が、テストに凍結した Stage 9 の `stepShooter` の写しと完全一致（分割リロードの 15 体も名指しで）                                                                                                                                                                                            | 2026-09-23 |
| 1 パス目の退化                 | core `sim/__tests__/firstPass.test.ts`               | 射撃に効く効果の無い 6 編成（操作キャラあり / なし）で、射撃の列が `planShots`、時刻表が `planDynamicSchedule` と厳密一致。固定サイクル・バーストなしも同じ                                                                                                                                                                                   | 2026-09-23 |
| 射手が見る窓                   | 同上                                                 | フレーム f のバーストで付いたチャージ速度▲は f の射撃に効かず、f + 1 から効く（固定サイクルの 600f の射撃の次が 640f、その次から 22f 間隔）。戦闘開始時の窓は 1 発目のマガジンから効く                                                                                                                                                        | 2026-09-23 |
| ループの窓 = 作り直した窓      | 同上                                                 | バースト・フルバースト・射撃の回数のトリガーを混ぜた編成で、ループの中で登録した窓と `planBuffTimeline` の窓が一致                                                                                                                                                                                                                            | 2026-09-23 |
| CT 短縮                        | 同上                                                 | 同じフレームの発動の後に当たる（今撃った III も 141f 縮む）。3 回目以降は 141・162・191f が同じフレームに重なる。明けた CT は動かさず、過去には戻さない。固定サイクルでは効かない                                                                                                                                                             | 2026-09-23 |
| DSL                            | core `skills/__tests__/stage10.test.ts`              | 射撃系の stat は passive・timed の両方に書ける、`flat` は `maxAmmo` だけ、即時効果の必須項目と未知の項目、`lastShot` は毎回でも倍率ダメージに書ける（ほかの射撃の回数は毎回を弾いたまま）                                                                                                                                                     | 2026-09-23 |
| 録画 19・37・A・B の編成       | core `__tests__/stage10Team.test.ts`                 | 下の回帰に加え、4 編成とも sim と calc で時刻表・即時効果・倍率ダメージ・バーストが厳密一致、「実数」のグループのトリガー数が sim と一致、全グループの発数の和 = 射撃の列の長さ、総ダメージは編成 3%・枠 5% 以内                                                                                                                              | 2026-09-23 |
| 表示                           | ブラウザで手動（`get_page_text`・`javascript_tool`） | リター + デルタ + ユニ + ドレイク（宝物 3）で、リターの枠の「受けているバフ」に最大装弾数 +45.17%、ユニの最大装弾数 +1 発、「バースト CT −2.34 秒 … 5 回（実際に縮んだ計 11.75 秒）」などが出る。区間表のトリガー数に「（実数）」が 28 行付く。コンソールエラーなし（スクリーンショットはウィンドウが背面でタイムアウトしたので撮れていない） | 2026-09-23 |

### 既存の録画の弾数表示（新規撮影なし）

操作キャラの照準の横に残弾が出る。コンタクトシートは `plan/captures/frames/` に置いた。

| 録画 | 読めた数値                                                                       | モデル                                                                                             | 証拠フレーム                                          |
| ---- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 37   | リロード中にバースト → 003 → 010 → 017 → 020。フルバースト中の最大 20            | 9 × (1 + 0.7218 + 0.5014) = 20.01 → 20（加算）。1 回分 = round(最大 × 0.33) を込めるたびに決める   | `20260923-37_…_f1250-1400_ammo-3-10-17-20.jpg`        |
| 19   | フルバースト開始で 009 → 014。終了直前に始まったリロードが 000 → 005 → 008 → 009 | 最大 9 + 5 = 14、弾丸チャージ floor(14 × 0.3988) = 5。1 回分 5（最大 14）→ 3（最大 9）→ 9 で止まる | `20260922-19_…_f1230-1950_ammo-9-14_reload-5-8-9.jpg` |

回帰テストでは、録画 37 の編成でドレイクのマガジンの最大が 20（基礎版のバーストだけなら 15）、録画 19 の編成でフルバースト開始ごとに SG の 2 体へ +5 発・デルタ（SR 6 + 5 = 11）へ +4 発の弾丸チャージ、を固定した。射手の単体テストでは 3 → 10 → 17 → 20 と 0 → 5 → 8 → 9 をフレーム単位で固定した。

### 数値への影響（180 秒・スペック固定・コア命中率 0・距離ボーナスあり・灼熱の的）

「前」は Stage 10 の語彙（射撃系の stat・即時効果）を定義から外した値。

| 編成                                                      | 前          | 後          | 倍率   | sim − calc | 変わったところ                                                                         |
| --------------------------------------------------------- | ----------- | ----------- | ------ | ---------- | -------------------------------------------------------------------------------------- |
| 録画 37（ラム + デルタ + ドレイク宝物 3、ドレイク操作）   | 215,958,055 | 219,546,927 | ×1.017 | +0.58%     | ドレイク 179,438,477 → 183,027,348（フルバースト中のマガジン 9 → 20 でリロードが減る） |
| 録画 19（エーテル + デルタ + ノワール、ノワール操作）     | 195,971,564 | 197,873,347 | ×1.010 | +0.77%     | ノワール S2（+5 発・弾丸チャージ）が 3 体とも。エーテル +2.0%、デルタ +2.3%            |
| 録画 A（リター + デルタ + ドレイク宝物 3、ドレイク操作）  | 298,771,470 | 302,752,247 | ×1.013 | +1.48%     | フルバーストは 5 回のまま、間隔が 40 秒 → 37.65・36.18・35.83・35.83 秒（下記）        |
| 録画 B（ラム + アドミ + ユニ + クイーン（真）、ラム操作） | 238,051,534 | 251,951,097 | ×1.058 | +0.13%     | アドミのリロード速度・ユニのチャージ速度と +1 発。クリダメ（アドミ）も入ったぶん大きい |

- **録画 A の予測は「フルバースト 5 回のまま、間隔が縮む」**（設計書 7 節の「6 回前後」を訂正）。リターは CT 20 秒の I なので、ゲージが満タンになるたびにすぐ撃ち、II（CT 40 秒）が明けないうちに 600f でチェーンが切れる（180 秒で 5 回）。CT 短縮はフルバーストの時刻を早めるが、早まる量はチェーンの切れ目の位相で決まり、短縮の量そのものではない。撮影では「チェーンの切れる回数」と「フルバーストの間隔」の両方を読む。

### まだ確かめていないこと（録画 A・B で読む）

| 仮の定数                   | 場所                                 | 仮の値                                     | 読むもの                                                                         |
| -------------------------- | ------------------------------------ | ------------------------------------------ | -------------------------------------------------------------------------------- |
| MAX_AMMO_ROUNDING          | `packages/core/src/sim/firing.ts`    | 切り捨て                                   | 録画 A のリターのバースト後 5 秒のデルタの最大（8 なら切り捨て、9 なら四捨五入） |
| MAX_AMMO_CLAMP_ON_DECREASE | `packages/core/src/sim/shooter.ts`   | 削る                                       | 録画 A のリターの 5 秒窓が切れた瞬間のリター・デルタの残弾                       |
| SPEED_FORMULA              | `packages/core/src/sim/firing.ts`    | `× (1 − 速度)`                             | 録画 B のラムのリロード（59f か 80f）とチャージ間隔（77f か 78f）                |
| CT 短縮の順番              | `packages/core/src/sim/firstPass.ts` | 同じフレームの発動の後（III の CT も縮む） | 録画 A のフルバーストの間隔とチェーンの切れる回数                                |
| AMMO_REFILL_ROUNDING       | `packages/core/src/sim/firstPass.ts` | 切り捨て                                   | 残弾の少ないときに弾丸チャージが来る場面（録画 19 は満タン近くで区別できない）   |
| AI の「フルチャージ」      | `data/skills/160.json` の assumes    | 数える                                     | 録画 B のラムの最大装弾数が 6 → 7 になるか（ユニ S2）                            |

## 較正した定数

| 定数                        | 場所                                      | 既定              | 較正値                                           | 根拠                                                                                                                    |
| --------------------------- | ----------------------------------------- | ----------------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| chargeReleaseFrames         | `packages/core/src/weapons.ts`            | 0                 | 22                                               | SR（デルタ）・RL（フラワー、ベロータ）の録画。チャージ完了から発射まで一律 +22f                                         |
| spinUpFirstShotFrames       | `packages/core/src/weapons.ts`            | （新設）          | 20                                               | MG 録画の装填完了→1 発目                                                                                                |
| MG スピンアップ             | `packages/core/src/cadence.ts`            | 切り上げ          | レート蓄積（CDN 値のまま）                       | MG 録画の 1 発目→300 発目 387f                                                                                          |
| BURST_GAUGE_MAX             | `packages/core/src/burst/controller.ts`   | 1,000,000         | 1,000,000                                        | CDN の `target_burst_energy_pershot` と同じ単位。倍率は下の BURST_ENERGY_MULTIPLIER で持つ                              |
| BURST_ENERGY_MULTIPLIER     | `packages/core/src/burst/dynamic.ts`      | （新設）          | 1.2                                              | 単騎の録画 23〜35。AR・MG・RL・SR とも 1 発が CDN 値の 1.17〜1.21 倍（コア / 胴体で差なし）                             |
| SG_PELLET_GAUGE_HIT_RATE    | `packages/core/src/burst/dynamic.ts`      | （新設）          | 0.75                                             | 録画 26・27。ノワールの 1 トリガー 8.1〜8.4% / 全ペレット命中の予測 10.8%                                               |
| フルチャージ倍率の対象      | `packages/core/src/burst/dynamic.ts`      | 全員              | 操作キャラだけ                                   | 録画 25。AI のデルタは 1 発 6.2〜7.1%（倍率なし）、操作中は 15.9%（録画 23・24）                                        |
| BURST_READY_DELAY_FRAMES 等 | `packages/core/src/burst/controller.ts`   | 20 / 20 / 20      | **未較正**（仮）                                 | 録画 18 の 60f 刻みの目視。満タン → I → II → III                                                                        |
| BURST_CHAIN_TIMEOUT_FRAMES  | `packages/core/src/burst/controller.ts`   | 600               | 600（目視）                                      | 録画 22 で II の後・I の後とも約 600f でチェーンが切れた                                                                |
| SKILL_HIT_FULL_BURST_BONUS  | `packages/core/src/skills/burstDamage.ts` | （新設）false     | **true**（フルバースト中の倍率ダメージだけ）     | 録画 37・38。S2 / ペレットの比がフルバーストの内外で同じ 4.5987、イサベルの追加ダメージ 758,766 = 299.7% × 1.5 × 1.3996 |
| フルバースト時間            | `packages/core/src/burst/dynamic.ts`      | 全員 600f         | III の `burst_duration`                          | 説明文「フルバーストタイム 5 秒▲▼」と CDN の 1,500 / 500 の対応。録画 38 でイサベルの 5 回とも 300f                     |
| 分割リロードの 1 回分       | `packages/core/src/sim/firing.ts`         | 回数 × 時間の一括 | 1 回分ずつ、round(その時点の最大 × reloadBullet) | 録画 37（3 → 10 → 17 → 20）・録画 19（0 → 5 → 8 → 9）。最大が一定なら込め終わる時刻は従来と同じ                         |
| 最大装弾数の比率            | `packages/core/src/sim/firing.ts`         | （新設）          | 加算（丸めは未確定）                             | 録画 37: 9 × (1 + 0.7218 + 0.5014) = 20（掛け算なら 23）。丸め・切れた時の削りは録画 A で確定する                       |
