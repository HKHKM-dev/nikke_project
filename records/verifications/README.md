# 検証記録

- 1 回の検証（1 つの問いについて、撮って・読んで・比べて・結論を出すまで）を 1 ファイルにする。設計は [design-stage20.md](../../plan/design-stage20.md) 3.2・3.2.1 節。
- ファイル名は `V-NNNN-<短い名前>.md`（短い名前は英小文字・数字・`-`）。番号は通し番号で、抜けは許す。並行したブランチで同じ番号を使ったときの手順は、設計書 3.6 節。
- 冒頭の箇条書きと本文の見出しは、下のひな形のとおりに書く。合わない行は `npm test` で落ちる。一覧は `npm run records:check` で [plan/verifications.md](../../plan/verifications.md) に生成する。
- 大きさは 30KB を目安にする。超えそうなら問いを分ける。
- 観測値にできる数値は観測値（`records/observations/`）に置き、観測値の `source` にこの記録の ID を書く。本文では ID で指し、同じ数値をほかの文書に書き写さない。
- 閉じた（完了・打ち切りの）記録は書き換えない。訂正は新しい記録を作り、その冒頭の「訂正」に古い記録の ID を書く。

## ひな形

```markdown
# V-NNNN: 題名

- 問い: 何を知りたいか（1 文）
- 話題: 結論の話題の語彙のどれか（plan/claims.md の節の名前）
- 日付: YYYY-MM-DD
- Stage: あれば（設計書と節）
- 録画: `NNN`、`NNN`〜`NNN`
- 結論: `C-NNNN`（完了のときだけ。打ち切りでは範囲外の結論だけ）
- 状態: 調査中
- 依存: `V-NNNN`（保留で、別の検証の結果を待つとき）
- 待ち: 撮影・育成などを待つとき、その内容（保留では依存か待ちのどちらかが要る）
- 派生元: `V-NNNN`（別の検証の途中で出た問いのとき）
- 訂正: `V-NNNN`（古い記録を訂正するとき）

## 条件

## 予測（撮る前に書く）

仮説ごとに、この録画で何が見えるはずか。この録画で仮説を見分けられるか。外れたときの影響の大きさは分かる範囲で。探索の撮影なら「探索」と書き、見えそうなものだけ書く。

## 読み方

## 結果

## 分かったこと・分からないこと

## 次に撮るもの
```

使わない項目の行は消す（空のまま残さない）。

## 状態

| 状態       | 冒頭の「結論」                                               | ほかに要るもの                         |
| ---------- | ------------------------------------------------------------ | -------------------------------------- |
| `調査中`   | 書かない                                                     | —                                      |
| `保留`     | 書かない                                                     | 「依存」か「待ち」                     |
| `完了`     | 1 つ以上（新しく作った結論か、裏付けた既存の結論）           | —                                      |
| `打ち切り` | 書かない。モデルで扱わないと決めたときだけ範囲外の結論を書く | 「分かったこと・分からないこと」の本文 |
